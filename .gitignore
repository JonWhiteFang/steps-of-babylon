*.iml
.gradle
.kotlin
/local.properties
/.idea
.DS_Store
/build
/app/build
/baselineprofile/build
/macrobenchmark/build
/captures
.externalNativeBuild
.cxx
local.properties
# /app/schemas
run-gradle.sh

# Transient build output (the CLAUDE.md/run-gradle workflow redirects gradle output here)
build.log

# Release signing (anchored to the top-level signing dir — must NOT match docs/release/)
/release/
keystore.properties
*.jks
*.keystore

# Android Developer Verification proof-of-ownership snippet (account-specific, one-time use).
app/src/main/assets/adi-registration.properties

# repowise API keys (local)
.repowise/.env
.superpowers/

# ktlint cache (CI downloads here)
.ktlint/
