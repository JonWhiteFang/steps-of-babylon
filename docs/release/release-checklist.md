# Release Checklist — Steps of Babylon

> **Reusable, version-agnostic checklist.** The one-time-setup section records facts that stay true
> across releases (done during v1.0.0, 2026-05-19). The per-release section is a template — copy it
> for each new release. The live build/release pipeline is largely automated (CI release lane, Plan 32 /
> ADR-0018); much of "Build Configuration" is now enforced in `app/build.gradle.kts` + `release.yml`.
> Never hardcode the version or test count here — read them from `app/build.gradle.kts` and
> STATE.md/CLAUDE.md.

## One-time setup (done during v1.0.0, 2026-05-19 — stays true)

- [x] `isMinifyEnabled = true` for release build type
- [x] `isShrinkResources = true` for release build type
- [x] ProGuard/R8 rules hardened (Room, Hilt, SQLCipher, Health Connect, sensors, WorkManager)
- [x] `fallbackToDestructiveMigration` removed (verified absent in `AppDatabase.kt` — real migrations only)
- [x] Upload keystore generated (`release/upload-keystore.jks`)
- [x] `keystore.properties` created with credentials
- [x] Keystore files in `.gitignore`
- [x] Play App Signing enrollment

## Per-release Build Configuration

- [ ] `versionCode` bumped (Play rejects reused codes) + `versionName` set in `app/build.gradle.kts`, committed before tagging
- [ ] `PLAY_LICENSE_KEY` secret present in the `release` environment (the build hard-fails on a blank key for release)

## Documentation

- [x] Privacy policy written (`site/index.md` — canonical; published to GitHub Pages via `.github/workflows/pages.yml`)
- [x] Play Store listing text written (`docs/release/play-store-listing.md`)
- [x] Signing guide written (`docs/release/signing-guide.md`)
- [ ] CHANGELOG section added for the release being shipped (per-release)
- [ ] Release notes doc added under `docs/release/` for the release being shipped (per-release)

## Pre-Release Verification

- [ ] Release APK installs and runs on API 34 device/emulator
- [ ] Release APK installs and runs on API 36 device/emulator
- [ ] Step counting works in background (release build)
- [ ] Health Connect permissions and step reading works
- [ ] No ANRs or crashes in 30-minute play session
- [ ] R8 didn't break any functionality (all screens load, battle runs, notifications fire)
- [ ] All notification channels work
- [ ] Widget renders correctly
- [ ] Battery usage acceptable (< 5% per day for step counting)
- [ ] Full JVM unit-test suite + instrumented suite pass (live counts in STATE.md / CLAUDE.md — don't hardcode here)

## Play Store Assets (Plan 31)

- [x] App icon: 512×512 PNG — `docs/release/store-assets/play-store-icon-512.png` (3.8 KB, generated by `tools/render_play_store_icon.py` from the same coordinates as the in-app vector adaptive icon; re-runnable)
- [x] Feature graphic: 1024×500 PNG — `docs/release/store-assets/play-store-feature-graphic-1024x500.png` (621.5 KB). User-provided `StepsOfBabylonArt.png` (1376×768 pixel-art Tower of Babel scene) center-vertical-cropped to 1376×672 (y=48..720), then LANCZOS-downscaled to 1024×500. Source preserved at `docs/release/store-assets/StepsOfBabylonArt.png`.
- [ ] Screenshots: minimum 2, recommended 8
- [x] Privacy policy hosted at public URL (GitHub Pages — <https://jonwhitefang.github.io/steps-of-babylon/>)

## Build Outputs

- AAB: `app/build/outputs/bundle/release/app-release.aab`
- APK: `app/build/outputs/apk/release/app-release.apk`
