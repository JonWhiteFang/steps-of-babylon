# Source File Index

All paths relative to `app/src/main/java/com/whitefang/stepsofbabylon/`.

## Application & DI

```
StepsOfBabylonApp.kt              # @HiltAndroidApp, Configuration.Provider (HiltWorkerFactory). #190 REL-1: onCreate installs a chaining Thread.setDefaultUncaughtExceptionHandler via the top-level JVM-testable `buildCrashHandler(previous, record)` — records a breadcrumb (injected CrashBreadcrumbStore) FIRST, then delegates to the previous handler (preserves Play vitals); never throws.
di/DatabaseModule.kt               # Hilt: Room DB (SQLCipher) + 13 DAO providers
di/RepositoryModule.kt             # Hilt: 8 repository interface → impl bindings (@Singleton)
di/StepModule.kt                   # Hilt: SensorManager provider
di/HealthConnectModule.kt          # Hilt: Health Connect organizational module
di/BillingModule.kt                # Hilt: @Binds BillingManager → BillingManagerImpl (C.5 PR 3: stub deleted, single binding for debug + release). Sibling BillingInternalModule @Binds BillingClientAdapter → RealBillingClientAdapter + companion @Provides PurchaseVerifier → RealPurchaseVerifier(BuildConfig.PLAY_LICENSE_KEY) (#124). Both modules `internal`.
di/AdModule.kt                     # Hilt: @Binds RewardAdManager → RewardAdManagerImpl (C.6 PR 3: stub deleted, single binding for debug + release). Sibling AdInternalModule @Binds RewardedAdAdapter → RealRewardedAdAdapter and ConsentManager → RealConsentManager. Both modules `internal`.
di/TimeModule.kt                   # Hilt: TimeProvider → SystemTimeProvider (B.1)
di/CoroutineScopeModule.kt         # Hilt: @ApplicationScope qualifier + app-lifetime CoroutineScope(SupervisorJob + Dispatchers.Default) for work that outlives VM cancellation (B.3 PR 2)
```

## Data Layer — Room

```
data/local/AppDatabase.kt         # @Database: 13 entities, 13 DAOs, version 12, exportSchema=true
data/local/Migrations.kt          # Registered Migration objects (v7→8 for battleStepsEarned / ADR-0003, v8→9 for billing_receipt / ADR-0005, v9→10 for ultimate_weapon_state recreate-table + bossPsEarnedToday / ADR-0008 + ADR-0009, v10→11 for card_inventory copyCount aggregation / ADR-0010, v11→12 for daily_mission (date,missionType) dedup + unique index / #127). #127: MIGRATION_11_12 recreate-table dance — GROUP BY (date,missionType) keeping MIN(id) PK + MAX() of each state column (MAX(claimed) prevents resurrecting a claimed duplicate), then CREATE UNIQUE INDEX index_daily_mission_date_missionType. #237: MIGRATION_FLOOR=7 const + pure AppMigrations.validateChain(migrations, liveVersion, floor) → list of chain problems (contiguity / +1-step / floor / tops-out-at-liveVersion), guarded by MigrationChainTest.
data/local/Converters.kt          # @TypeConverters: Map<Int,Int> and Map<String,Int> via JSON
data/local/DatabaseKeyManager.kt  # SQLCipher passphrase via Android Keystore. #238: decrypt-failure response scoped to cause — pure decideOnDecryptFailure(aliasExists) → Wipe (alias provably absent = device restore) / Rethrow (alias present = transient fault, preserve progress); injectable keystoreAliasExists seam (defaults to "present"/no-wipe if keystore can't be opened).
data/local/PlayerProfileEntity.kt # Player profile entity (single row, id=1)
data/local/PlayerProfileDao.kt    # Player DAO: get() as Flow, atomic currency adjustments (incl. adjustStepBalanceIfSufficient SQL-guarded deduct for B.2 PR 1, spendGemsAtomic + spendPowerStonesAtomic SQL-guarded deducts for V1X-10 — both deduct + increment-spent in single statement, return rowCount for caller success check)
data/local/WorkshopUpgradeEntity.kt # Workshop upgrade entity
data/local/WorkshopDao.kt         # Workshop DAO + @Transaction purchaseUpgradeAtomic default method (B.2 PR 1)
data/local/LabResearchEntity.kt   # Lab research entity
data/local/LabDao.kt              # Lab research DAO
data/local/CardInventoryEntity.kt # Card inventory entity
data/local/CardDao.kt             # Card inventory DAO (+@Transaction openCardPackAtomic: guarded Gem deduct + 3 card writes in one tx, #236/ADR-0027)
data/local/UltimateWeaponStateEntity.kt # UW state entity (R4-06: weaponType PK + damageLevel + secondaryLevel + cooldownLevel + isUnlocked + isEquipped)
data/local/UltimateWeaponDao.kt   # UW state DAO (+markUnlocked +updateDamageLevel/updateSecondaryLevel/updateCooldownLevel for R4-06 per-path upgrades; +@Transaction unlockWeaponAtomic: guarded Power-Stone deduct + unlock in one tx, #236/ADR-0027)
data/local/DailyStepRecordEntity.kt # Daily step record entity (with escrow fields)
data/local/DailyStepDao.kt        # Daily step record DAO (with escrow queries) + @Transaction creditBattleStepsAtomic default method (B.2 PR 2); incrementBattleSteps UPSERT SQL supplies all 9 NOT NULL columns explicitly on the INSERT half so the fresh-install first-kill path doesn't crash on NOT NULL before ON CONFLICT can resolve UNIQUE (2026-05-12 hotfix); +creditBossPowerStonesAtomic (R4-07) mirrors the battle-step shape for boss-drop PS with 100/day cap. #121: +4 column-targeted upserts (setSensorAndCreditedSteps / setHealthConnectSteps / setActivityMinutes / setEscrow) mirroring the incrementBattleSteps template — each `INSERT … ON CONFLICT(date) DO UPDATE SET <only-its-columns>` so concurrent disjoint-column writers (sensor service / sync worker / HC validator) can't clobber each other (replaces the per-field read-copy-upsert; SET-not-increment, no schema change)
data/local/WalkingEncounterEntity.kt # Walking encounter entity
data/local/WalkingEncounterDao.kt # Walking encounter DAO
data/local/WeeklyChallengeEntity.kt # Weekly step challenge entity
data/local/WeeklyChallengeDao.kt   # Weekly challenge DAO
data/local/DailyLoginEntity.kt     # Daily login tracking entity
data/local/DailyLoginDao.kt        # Daily login DAO
data/local/MilestoneEntity.kt      # Milestone claim state entity
data/local/MilestoneDao.kt         # Milestone DAO + @Transaction claimMilestoneAtomic default method (B.2 PR 4)
data/local/DailyMissionEntity.kt   # Daily mission entity. #127: unique index on (date, missionType) — the authoritative guard against duplicate daily missions (the generator's read-then-insert check is racy on a WAL pool).
data/local/DailyMissionDao.kt      # Daily mission DAO. #127: insert is @Insert(onConflict=IGNORE) on the (date,missionType) unique index; new @Transaction generateForDate(date, missions) batches the emptiness-check + inserts (the index, not the transaction, closes the TOCTOU window). markClaimed guarded claim (#122). Note: @Upsert is dead/unused (incompatible with the unique index — don't wire it).
data/local/CosmeticEntity.kt       # Cosmetic store entity
data/local/CosmeticDao.kt          # Cosmetic store DAO (#221: + deleteByIds(ids) for the dead-cosmetic purge)
data/local/BillingReceiptEntity.kt # Play Billing receipt entity — idempotency store keyed by purchaseToken (C.5 PR 1 / ADR-0005)
data/local/BillingReceiptDao.kt    # Billing receipt DAO + @Transaction grantOnceAtomic default method (C.5 PR 1)
```

## Data Layer — Repositories

```
data/repository/PlayerRepositoryImpl.kt         # Player profile + wallet (entity→domain mapping). #26: observe* flows now pipe through `distinctUntilChanged()` so downstream VMs don't recompose on equal-value Room emissions (reduces unnecessary redraws on the hot step-count path).
data/repository/WorkshopRepositoryImpl.kt        # Workshop upgrades; takes WorkshopDao + PlayerProfileDao for purchaseUpgradeAtomic (B.2 PR 1)
data/repository/LabRepositoryImpl.kt             # Lab research
data/repository/CardRepositoryImpl.kt            # Card inventory
data/repository/UltimateWeaponRepositoryImpl.kt  # Ultimate weapon state (R4-06: toDomain rewrite for 4-column entity → 6-field OwnedWeapon; upgradePathLevel dispatches to per-path DAO methods)
data/repository/StepRepositoryImpl.kt            # Daily step records + escrow + getDailyRecord(). #121: the 4 per-field updates (updateDailySteps / updateHealthConnectSteps / updateActivityMinutes / updateEscrow) delegate to the DAO's column-targeted upserts instead of read-copy-upsert, so concurrent writers no longer clobber each other's columns. #227: now injects PlayerProfileDao too + exposes sumCreditedSteps / creditBattleStepsAtomic / creditBossPowerStonesAtomic (handed into the DAO @Transaction with the real PlayerProfileDao so the wallet credit stays in one DB-scoped transaction)
data/repository/WalkingEncounterRepositoryImpl.kt # Walking encounters
data/repository/MissionRepositoryImpl.kt         # (#227) wraps DailyMissionDao; maps DailyMission↔DailyMissionEntity, null-skipping rows whose stored missionType no longer resolves to a DailyMissionType (no valueOf crash); generateForDate preserves the @Transaction batch + (date,missionType) unique-index + IGNORE guard (#127)
data/repository/MilestoneRepositoryImpl.kt       # (#227) wraps MilestoneDao + PlayerProfileDao; getClaimedMilestoneIds + claimMilestoneAtomic (hands the real PlayerProfileDao into the DAO @Transaction — guarded-deduct preserved, ADR-0027)
data/repository/DailyLoginRepositoryImpl.kt      # (#227) wraps DailyLoginDao; maps DailyLogin↔DailyLoginEntity
data/repository/WeeklyChallengeRepositoryImpl.kt # (#227) wraps WeeklyChallengeDao; maps WeeklyChallenge↔WeeklyChallengeEntity
data/repository/CosmeticRepositoryImpl.kt        # Cosmetic store items + private ZIGGURAT_COLOR_LOOKUP table (5 palettes: zig_jade @ C.2 PR 2, lapis_lazuli_skin @ PR 3 / IRON_SOLES reward, garden_ziggurat_skin @ PR 3b / MARATHON_WALKER reward, sandals_of_gilgamesh @ PR 3c / GLOBE_TROTTER reward, zig_obsidian @ V1X-14 / store-purchasable dark skin); resilient toDomainOrNull maps CosmeticItem.overrideColors from the lookup and drops rows whose stored category no longer parses (#221 — never crashes on a legacy/dead row); SEED_COSMETICS: 7 ZIGGURAT_SKIN rows (incl. the 3 milestone-reward cosmetics — #221 removed the 2 PROJECTILE_EFFECT + 2 ENEMY_SKIN dead rows); ensureSeedData uses a per-cosmeticId filter (content PRs land on already-seeded installs without a data clear, player `isOwned`/`isEquipped` survives upgrades) + a deleteByIds(DEAD_COSMETIC_IDS) purge that cleans the 4 dead rows off already-installed devices
```

## Data Layer — Sensor

```
data/sensor/StepSensorDataSource.kt  # TYPE_STEP_COUNTER wrapper, emits deltas via callbackFlow (+isSensorAvailable() for the #193 no-sensor onboarding signal)
data/sensor/BatteryOptimizationStatus.kt  # #261/ADR-0029: @Singleton PowerManager wrapper; isIgnoring() = isIgnoringBatteryOptimizations(packageName). Injectable seam (mirrors StepSensorDataSource) for the onboarding battery-exemption primer; mocked directly in JVM tests
data/sensor/StepRateLimiter.kt       # Rolling 1-min window rate limiter (200/min, 250 burst)
data/sensor/StepVelocityAnalyzer.kt  # Unnatural step pattern detection (shaker/spoof), penalty multiplier
data/sensor/StepIngestionPreferences.kt # Service heartbeat + day-start counter for worker/service coordination. #123: +sensorStepsAtDayStart offset (set alongside the baseline, getSensorStepsAtDayStart) so the worker measures the credited-steps gap relative to the baseline — survives a mid-day reboot re-anchor
data/sensor/DailyStepManager.kt      # Orchestrates: rate limit → velocity analysis → STEP_MULTIPLIER (Workshop) + STEP_EFFICIENCY (Lab) bonus combined under shared +100 % cap (sensor walking only, RO-08 + RO-11 #A.3) → 50k ceiling → Room persist + activity minutes; constructor takes WorkshopRepository + LabRepository. #120: recordSteps/recordActivityMinutes run their full read-check-write under a kotlinx Mutex (ensureInitializedLocked is the non-reentrant init split) so the ceiling RMW is atomic across the service + worker threads; stepsPerMinute is a ConcurrentHashMap, counters @Volatile. Guarded by DailyStepManagerConcurrencyTest (deterministic onBeforeCreditCommit seam). #251: `recordTrustedSteps(rawDelta, timestampMs)` is a trusted batch-credit path for HC-verified offline-recovery gaps — runs under the SAME non-reentrant #120 mutex (calls ensureInitializedLocked, never recordSteps), SKIPS rate-limit + velocity + per-minute tracking, KEEPS the 50k ceiling + STEP_MULTIPLIER; persists the raw gap into dailySensorTotal (idempotent fillGaps). Guarded by 5 R251 DailyStepManagerTest cases. #232: the 4 follow-on-pipeline catch blocks (widget/supply-drop/economy/mission) no longer swallow silently — they report via the `onPipelineError(stage,e)` seam (default Log.w, runs under the #120 mutex so overrides must be non-blocking/non-throwing); guarded by DailyStepManagerErrorReportingTest. #211 (ADR-0036): the **single time-tamper baseline owner** — under the #120 mutex it `TimeIntegrity.evaluate(antiCheatPrefs.readTimeBaseline(), antiCheatPrefs.currentTimeReading())`, persists the advanced baseline via `writeTimeBaseline` (apply() can't abort the credit), and passes `isRollback = verdict is TimeVerdict.Rollback` into TrackDailyLogin. VMs are read-only consumers (never persist).
```

## Data Layer — Billing & Ads

```
data/billing/BillingManagerImpl.kt             # Real Play Billing v9 impl (C.5 PR 1 / ADR-0005): adapter + receipt DAO + wallet credits + SHA-256 obfuscatedAccountId anti-fraud; `internal`; wallet side-effects run inside `BillingReceiptDao.grantOnceAtomic`; consume/ack runs after tx commits; `reconcilePendingPurchases` overrides the BillingManager default no-op to sweep PENDING→PURCHASED transitions + retry unresolved consume/ack. **#124:** both grant paths (handleCompletedPurchase + reconcileType) call `verifier.isValidPurchase(originalJson, signature, product.skuId(), purchaseToken)` BEFORE grantOnceAtomic — rejects forged/replayed purchases with no receipt/credit. Sole `BillingManager` binding post-C.5 PR 3 (`StubBillingManager` deleted after Phase G internal-track on-device verification PASSED 2026-05-18).
data/billing/internal/BillingClientAdapter.kt  # SDK-neutral seam for `BillingManagerImpl` (C.5 PR 1). Interface + sealed result types (SdkBillingResult / SdkPurchase / SdkProductDetails / QueryProductDetailsResult / StartPurchaseResult / QueryPurchasesResult); `internal`; tests mock this directly so no `com.android.billingclient.*` imports leak into unit tests. **#124:** `SdkPurchase` now carries nullable `originalJson` + `signature` (the bytes Google signed + its RSA-SHA1 signature).
data/billing/internal/PurchaseVerifier.kt      # **#124** SDK-neutral seam for client-side Play purchase signature verification. Interface `isValidPurchase(originalJson, signature, expectedProductId, expectedPurchaseToken)`; `internal`; tests substitute a fake. Distinct from the server-side verification forbidden by CONSTRAINTS.md (needs no backend).
data/billing/internal/RealPurchaseVerifier.kt  # **#124** Concrete PurchaseVerifier: pure-JVM `SHA1withRSA` verify (port of the Play Billing `Security.verifyPurchase` sample) against the Base64 Play "Licensing" key, PLUS signed-productId/purchaseToken binding (blocks replaying a signed cheap receipt for an expensive product). Blank key → fail-open (debug/CI); unparseable key → fail-closed; never throws. JVM-unit-tested against a real RSA keypair (`RealPurchaseVerifierTest`).
data/billing/internal/RealBillingClientAdapter.kt # Concrete BillingClientAdapter backed by Google Play Billing Library v8 (C.5 PR 1). Uses `enableAutoServiceReconnection()`, `PendingPurchasesParams.enableOneTimeProducts`, Mutex-guarded `launchPurchase` with CompletableDeferred bridging PurchasesUpdatedListener. Device-only testable — the only file in the app that imports `com.android.billingclient.*`. **#124:** `toSdk()` now populates `SdkPurchase.originalJson` + `signature` from `Purchase.getOriginalJson()`/`getSignature()`.
data/billing/internal/ActivityProvider.kt      # WeakReference-backed Singleton Activity holder. MainActivity.onResume sets + onPause clears (C.5 PR 2).
data/ads/RewardAdManagerImpl.kt                # Real AdMob impl (C.6 PR 1 / ADR-0006): adapter + consent manager + per-placement BuildConfig ad-unit routing; `internal`; session-Mutex-guarded; consent → load → show → map to AdResult. Shares the existing `data/billing/internal/ActivityProvider` for the Activity reference needed by `RewardedAd.show()`. Sole `RewardAdManager` binding post-C.6 PR 3 (`StubRewardAdManager` deleted).
data/ads/internal/RewardedAdAdapter.kt         # SDK-neutral seam for `RewardAdManagerImpl` (C.6 PR 1). Interface + sealed result types (SdkAdLoadResult / SdkAdShowResult / SdkRewardedAd); `internal`; tests mock this directly so no `com.google.android.gms.ads.*` imports leak into unit tests.
data/ads/internal/RealRewardedAdAdapter.kt     # Concrete RewardedAdAdapter backed by Google Mobile Ads SDK v25 (C.6 PR 1). Lazy `MobileAds.initialize` on first loadAd, CompletableDeferred bridging `RewardedAdLoadCallback` + `FullScreenContentCallback` + `OnUserEarnedRewardListener`, AtomicBoolean `rewarded` flag flipped only inside `onUserEarnedReward`. Device-only testable — the only file in the app that imports `com.google.android.gms.ads.*`. #241/ADR-0032: `ensureSdkInitialized` now calls `MobileAds.setRequestConfiguration(buildAdRequestConfiguration())` before init; top-level pure `buildAdRequestConfiguration()` returns maxAdContentRating=PG (13+/no child-directed/no age-gate), JVM-tested by AdRequestConfigurationTest.
data/ads/internal/ConsentManager.kt            # SDK-neutral seam for UMP consent (C.6 PR 1). Interface; `internal`; tests mock directly so no `com.google.android.ump.*` imports leak into unit tests.
data/ads/internal/RealConsentManager.kt        # Concrete ConsentManager backed by UMP SDK v4 (C.6 PR 1). Mutex-guarded once-per-session `requestConsentInfoUpdate` + `loadAndShowConsentFormIfRequired`; errors logged but non-throwing. Device-only testable — the only file in the app that imports `com.google.android.ump.*`.
```

## Data Layer — Health Connect

```
data/healthconnect/HealthConnectClientWrapper.kt  # HealthConnectClient wrapper, availability, permissions
data/healthconnect/HealthConnectStepReader.kt      # Reads aggregated daily steps via aggregate()
data/healthconnect/StepCrossValidator.kt           # Cross-validation, graduated response (4 offense levels); 5 multi-write branches wrapped in AppDatabase.withTransaction via @VisibleForTesting runInTransaction seam (B.2 PR 3)
data/healthconnect/StepGapFiller.kt                # Recovers missed steps from HC when service killed (gap = hcTotal − sensorTotal). #251: credits the recovered gap via DailyStepManager.recordTrustedSteps (NOT recordSteps) so the live-walking rate limiter doesn't clamp a legitimate multi-hour batch recovery to ~200.
data/healthconnect/ExerciseSessionReader.kt        # Reads exercise sessions for Activity Minute Parity
data/healthconnect/ActivityMinuteConverter.kt      # Converts exercise minutes to step-equivalents with caps
data/healthconnect/ActivityMinuteValidator.kt      # Filters suspicious exercise sessions (duration/type/micro caps)
data/BiomePreferences.kt                          # SharedPreferences wrapper for first-seen biome tracking
data/DataDeletionManager.kt                       # Full local data wipe: WorkManager cancel + service stop + DB delete + 15 SharedPreferences clear (PREFS_NAMES; +onboarding_prefs +haptics_prefs #247; +supply_drop_notifications #216) + Keystore alias delete + Activity recreate (V1X-01). #248: cancelAllWork().result is now AWAITED (bounded .get(2s), main-thread-safe) BEFORE database.close() so an in-flight StepSyncWorker can't write to a closed DB; on timeout/error it logs + proceeds. PREFS_NAMES guarded by DataDeletionPrefsCoverageTest (source scan; fails on a drifted/unresolvable getSharedPreferences site).
data/MilestoneNotificationPreferences.kt           # SharedPreferences wrapper for milestone notification dedup
data/NotificationPreferences.kt                   # SharedPreferences wrapper for 4 notification toggles
data/SoundPreferences.kt                          # SharedPreferences wrapper for sound mute/volume
data/HapticsPreferences.kt                        # SharedPreferences wrapper ("haptics_prefs") for the haptic on/off flag, default ON. Mirrors SoundPreferences; @Singleton constructor-injected (no Hilt module). Settings VM writes it; rememberHaptics() reads it. Bundle C (#162)
data/anticheat/AntiCheatPreferences.kt            # SharedPreferences wrapper for anti-cheat counters + CV offense tracking. #211: also the time-tamper baseline store — implements the pure-domain `TimeBaselineSource` (`currentTimeReading()` = SystemClock.elapsedRealtime()/System.currentTimeMillis(), the one Android-coupled time seam; `readTimeBaseline()` returns the persisted 4-slot `TimeBaseline` or null until first written) + a concrete `writeTimeBaseline()` (4 longs via `apply()`, async/non-throwing). Same `anti_cheat_prefs` plaintext MODE_PRIVATE prefs; wiped by DataDeletionManager's existing clear.
data/onboarding/OnboardingPreferences.kt          # SharedPreferences wrapper for the device-local first-launch onboarding-complete flag (NOT Room — must not sync)
data/diagnostics/CrashBreadcrumb.kt                # #190: immutable crash-breadcrumb model (timestampMillis/threadName/exceptionClass/message?/stackPreview)
data/diagnostics/CrashBreadcrumbStore.kt           # #190 REL-1: device-local single-slot crash breadcrumb (SharedPreferences `crash_breadcrumb_prefs`, mirrors OnboardingPreferences; NOT Room). record(thread, throwable, timestampMillis)/peek/clear; synchronous commit() (process may be dying); every method best-effort/never-throws; caller-supplied timestamp (no internal clock). MAX_STACK_CHARS=4096. Written by both the global handler (StepsOfBabylonApp) and the loop guard (GameLoopThread); wiped by DataDeletionManager.
```

## Data Layer — Time

```
data/time/SystemTimeProvider.kt       # Production TimeProvider backed by Instant.now() / LocalDate.now(). @Singleton @Inject.
```

## Data Layer — i18n

```
data/AndroidStrings.kt                # Production domain.Strings impl (V1X-13, ADR-0014): Context.getString/getQuantityString over the fx_*/enemy_*/wave_*/boss plural+string resources (8 methods incl. enemyTypeName/waveComposition/bossCountdown + the fx_step_reward plural, #260/#259). Constructed directly by GameSurfaceView (like SoundManager) — no Hilt binding.
```

## Domain Layer — Time

```
domain/time/TimeProvider.kt           # Wall-clock seam: now() / today(). No Android imports. Introduced by B.1.
domain/time/TimeIntegrity.kt          # #211 (TIME-1, ADR-0036): pure-domain clock-tamper decision core, no Android imports. `TimeIntegrity.evaluate(baseline, reading)` reasons over a persisted 4-slot `TimeBaseline(lastElapsedRealtime, lastWallClock, maxWallClockSeen, trustedWallClock)` + a fresh `TimeReading(elapsedRealtime, wallClock)` → `TimeVerdict.Trusted | Rollback` (always carries the advanced baseline). Backward-rollback guard: `reading.wallClock < maxWallClockSeen` ⇒ Rollback (reboot-durable floor). Forward-jump guard: `trustedWallClock` advances only by `min(wallDelta, elapsedDelta).coerceAtLeast(0)` — order-independent capped-accrual anchor (reboot `elapsedDelta<0` → falls back to full wallDelta, §2 accepted gap). Also defines the `TimeBaselineSource` read seam (`readTimeBaseline()`/`currentTimeReading()`) AntiCheatPreferences implements + a fake backs in VM tests. The Android clock reads live in the data layer. Guarded by TimeIntegrityTest.
domain/Strings.kt                     # i18n seam for engine-internal battle text (V1X-13, ADR-0014; #260). Pure-Kotlin interface, 8 methods (healHp/rapidFireBurst/cashReward/stepReward/powerStoneReward + enemyTypeName/waveComposition/bossCountdown), no Android imports (EnemyType is domain). Impl = data/AndroidStrings; consumed by GameEngine + BattleViewModel via nullable var with literal fallback.
domain/notification/NotificationPolicy.kt # #216 (NOTIF-1): pure-domain decision helper for suppressing proactive notifications. No Android imports (java.time only). Constants QUIET_HOURS_START/END (22:00–08:00 local) + SUPPLY_DROP_NOTIFICATION_DAILY_CAP (3). `isWithinQuietHours(now)` handles the midnight-crossing window [START,END); `canSendReminder(now)` = !quiet; `canSendSupplyDropNotification(now, sentToday)` = !quiet && sentToday < cap. The managers read the wall clock + own the per-day counter; this only decides. Guarded by NotificationPolicyTest.
```

## Domain Layer — Battle Engine (V1X-09)

```
domain/battle/engine/SimulationMath.kt # Pure-math helpers extracted from GameEngine. 10 functions: recoveryPulseAmount (V1X-09, ADR-0012), chronoMultiplier (V1X-09), thornReflectionDamage (V1X-09), lifestealHealAmount (V1X-09), tickLifestealAccumulator (V1X-09), clampHp (V1X-09), stepMultiplierBonus (V1X-18, ADR-0015 — asymptotic curve `1 - (1 - 0.05)^level`), clampAccumulator (#126 — bounds the fixed-timestep catch-up backlog at MAX_CATCHUP_TICKS whole ticks + sub-tick remainder; anti-spiral-of-death, wired into GameLoopThread), killCashReward + waveCompleteCash (#230/ADR-0012 Phase 4 — per-kill and wave-complete cash formulas hoisted verbatim from GameEngine, bit-identical, now called by CombatResolver). Plus 11 constants (RECOVERY_*, CHRONO_SLOW_FACTOR_DEFAULT, LIFESTEAL_CAP, STEP_MULTIPLIER_DECAY_FACTOR, STEP_MULTIPLIER_BONUS_CAP, MAX_CATCHUP_TICKS=8, BASE_CASH_PER_WAVE=20L + FLAT_BONUS_PER_WAVE_LEVEL=5L + CASH_BONUS_PER_LEVEL=0.03 — the last three moved from the deleted GameEngine companion in #230).
domain/battle/engine/Simulation.kt # V1X-09 Phase 3 (ADR-0012, slices 1–6). Pure-domain owner of in-round simulation state. Slice 1: cash economy — @Volatile cash / totalCashEarned (private set) + reset / creditCash (credits both) / applyInterest (cash only, min(level×0.5%,10%), excluded from lifetime) / spend (insufficient-balance guard). Slice 2: round-progress counters — @Volatile totalEnemiesKilled / totalStepsEarned / elapsedSeconds (private set) + tickElapsed / recordEnemyKilled / creditSteps (ignores non-positive) / hasWaveProgress. Slice 3: tickEntities(entities, deltaTime, chronoSlowFactor) scales dt for chrono-slowable (enemy) entities via the EntityProtocol seam, full dt otherwise (lifted from the engine's inline `is EnemyEntity` tick loop). Slice 4: collision sweep — generic detectProjectileEnemyHits<P,E>(projectiles, enemies, onHit) (first-overlap-per-projectile + break) + detectZigguratHits<P>(enemyProjectiles, zigX, zigY, zigWidth, onHit); both fire onHit interleaved INSIDE the loop (exact original overlap expressions) so a hit's knockback/death side-effects are observed by later projectiles the same frame — behaviour-identical to the old CollisionSystem loops. GameEngine holds a Simulation and delegates its cash / totalCashEarned / spendCash + the three counters + hasWaveProgress() + both collision sweeps here; roundOver (control flag) + per-kill / wave-complete reward *formulas* stay in GameEngine. Slice 5: UW lifecycle timers — advanceUWTimers(cooldownRemaining, effectTimeRemaining, deltaTime): UWTimerAdvance (cooldown floored at 0 + effect-duration countdown clamped to 0 on the crossing frame + justExpired/effectWasActive transition flags so the engine runs CHRONO/GOLDEN expiry once and BLACK_HOLE/POISON ongoing effects every active frame incl. the expiry frame) + isUWReadyToFire(...) auto-trigger predicate; GameEngine.updateUWs applies the result + keeps the presentation-coupled side-effects + activateUW (UWState + uwStates + public API unchanged). Slice 6: SimulationEvent flow — events: SharedFlow<SimulationEvent> (replay=0, extraBufferCapacity=64, DROP_OLDEST) + emit(event) (non-suspending tryEmit, game-loop-thread safe); GameEngine emits StepReward/BossKilled from handleEnemyDeath (replacing its two @Volatile callbacks) and BattleViewModel collects the stream. **Completes Phase 3.** No Android imports.
domain/battle/engine/SimulationEvent.kt # V1X-09 Phase 3 final slice (ADR-0012). Pure-domain sealed interface for the game-loop→ViewModel one-shot side-effect hand-off: StepReward(amount,x,y) + BossKilled(tier,x,y). Replaces GameEngine's @Volatile onStepReward/onBossKilled callbacks; emitted via Simulation.emit, collected by BattleViewModel.handleSimulationEvent. No Android imports.
```

## Domain Layer — Models

```
domain/model/PlayerWallet.kt          # Currency balances data class (Steps, Cash, Gems, Power Stones). Presentation-layer CurrencyType enum lives in presentation/ui/CurrencyDisplay.kt; the old domain Currency enum was deleted as dead (PR #165).
domain/model/PlayerProfile.kt         # Full profile (maps from PlayerProfileEntity)
domain/model/ActiveResearch.kt        # In-progress lab research
domain/model/OwnedCard.kt             # Player-owned card instance
domain/model/OwnedWeapon.kt           # Player-owned ultimate weapon (R4-06: 6 fields — type, damageLevel, secondaryLevel, cooldownLevel, isUnlocked, isEquipped + levelOf(path))
domain/model/UWPath.kt                # 3-value enum: DAMAGE, SECONDARY, COOLDOWN (R4-06 — used by UpgradeUltimateWeapon + UltimateWeaponRepository)
domain/model/DailyStepSummary.kt      # Daily step record domain model (with escrow fields)
domain/model/DailyMission.kt          # (#227) domain view of a daily_mission row (id, type: DailyMissionType, target, progress, rewards, completed, claimed) — removes the DailyMissionEntity import from the mission use cases
domain/model/DailyLogin.kt            # (#227) domain view of a daily_login row (date, stepsWalked, powerStoneClaimed, gemsClaimed)
domain/model/WeeklyChallenge.kt       # (#227) domain view of a weekly_challenge row (weekStartDate, totalSteps, claimedTier)
domain/model/SupplyDrop.kt              # Walking encounter supply drop
domain/model/SupplyDropTrigger.kt        # 3 trigger types with notification messages
domain/model/SupplyDropReward.kt         # 4 reward types (Steps, Gems, Power Stones, Card Copy)
domain/model/DropGeneratorState.kt       # Generator state tracking (lastCheckSteps, milestoneTriggered)
domain/model/Milestone.kt               # 6 walking milestones with step thresholds and rewards
domain/model/MilestoneReward.kt          # Sealed class: Gems, PowerStones, Cosmetic
domain/model/DailyMissionType.kt         # 6 daily mission types (walking/battle/upgrade); also declares the inline `enum class MissionCategory { WALKING, BATTLE, UPGRADE }`
domain/model/BillingProduct.kt           # 5 billing products + PurchaseResult sealed class + public `skuId()` returning `name.lowercase(Locale.ROOT)` (#262 L88: Locale.ROOT pins the wire id locale-independent — default-locale lowercase() corrupted GEM_PACK_MEDIUM's `I`→dotless ı under Turkish; Plan 31 Phase F unblocker, refines ADR-0005 decision #6 to the lowercase wire format Play Console requires) + opt-in Companion for data-layer `BillingProduct.fromSkuIdOrNull(skuId)` reverse lookup (C.5 PR 1)
domain/model/AdPlacement.kt              # 3 ad placements + AdResult sealed class
domain/model/CosmeticCategory.kt         # Cosmetic categories — ZIGGURAT_SKIN only (#221 removed the unused PROJECTILE_EFFECT/ENEMY_SKIN values that had no render path)
domain/model/CosmeticItem.kt             # Cosmetic item domain model (+ optional overrideColors: List<Int>? for renderer override, C.2 PR 1)
domain/model/UpgradeType.kt           # 24 Workshop upgrade types with configs (R4-03 added RAPID_FIRE in ATTACK)
domain/model/RapidFireSchedule.kt     # R4-03 helper: per-level (interval, duration, multiplier) interpolation table for the RAPID_FIRE upgrade. L1 60s/5s/2.0× → L10 30s/30s/3.0× (duration matches interval = permanent buff). Centralises math so GameEngine.tickRapidFire and DescribeUpgradeEffect.formatRapidFire read identical numbers.
domain/model/UpgradeCategory.kt       # Attack, Defense, Utility categories
domain/model/UpgradeConfig.kt         # Upgrade configuration (baseCost, scaling, maxLevel)
domain/model/Tier.kt                  # Tier data class
domain/model/TierConfig.kt            # Full tier table (1–10)
domain/model/BattleCondition.kt       # 7 battle condition types
domain/model/Biome.kt                 # 5 biomes with forTier() mapping
domain/model/BattleConditionEffects.kt # Pre-computed battle condition modifiers from tier
domain/model/EnemyType.kt             # 6 enemy types with multipliers
domain/model/UltimateWeaponType.kt    # 6 UW types with per-path L1/L10 spec + valueAtLevel/cooldownAtLevel/damageAtLevel/secondaryAtLevel/costForPath (R4-06)
domain/model/UltimateWeaponLoadout.kt # UW loadout (max 3)
domain/model/ResearchType.kt          # 12 lab research types; +`val isComingSoon: Boolean = false` constructor field (RO-11 #B.2); only AUTO_UPGRADE_AI flagged true with description "Reserved for v1.x — research progress preserved" (ENEMY_INTEL wired in V1X-15b: 8000×1.5 cost / 4h×1.10 / maxLevel 10 / +2%/lvl damage outer multiplier + L1/L5/L10 UI overlays per ADR-0017); the other 11 wired end-to-end (DAMAGE / HEALTH / CRITICAL / REGEN / CASH / STEP_EFFICIENCY / UW_COOLDOWN / ENEMY_INTEL as outer multipliers via ResolveStats + GameEngine + DailyStepManager; WAVE_SKIP via WaveSpawner.startWave; MULTISHOT_RESEARCH / BOUNCE_RESEARCH via ResolveStats caps). #44: `companion object fun surfacedInLabs(): List<ResearchType> = entries.filterNot { it.isComingSoon }` — single source of truth for which types the Labs UI shows (excludes coming-soon, order-preserving); LabsViewModel consumes it instead of an inline filter, guarded by ResearchTypeTest.
domain/model/CardRarity.kt            # Common, Rare, Epic
domain/model/CardType.kt              # 9 card types with effects + effectAtLevel/secondaryAtLevel/effectDescriptionAtLevel(level) — the description method (#53 fix) is the single source of truth for the live, level-aware UI string and stays in lockstep with the gameplay math by deriving its numbers from the same effectAtLevel formula
domain/model/CardLoadout.kt           # Card loadout (max 3)
domain/model/RoundState.kt            # Transient battle state
domain/model/ZigguratBaseStats.kt     # Base stat constants (HP, damage, attack speed, range, regen, projectile speed)
domain/model/ResolvedStats.kt         # Computed combat stats from workshop + in-round upgrades
```

## Domain Layer — Interfaces & Use Cases

```
domain/repository/PlayerRepository.kt          # Profile/wallet: observe + spend/add currencies. #122: spendGems/spendPowerStones return Boolean (rows-affected>0) + new spendStepsIfSufficient(): Boolean so callers gate grants on the guarded deduct; spendSteps stays Unit/clamp for the anti-cheat escrow clawback (ADR-0020)
domain/repository/WorkshopRepository.kt         # Workshop upgrades interface (incl. purchaseUpgradeAtomic for B.2 PR 1)
domain/repository/LabRepository.kt              # Lab research interface
domain/repository/CardRepository.kt             # Card inventory interface
domain/repository/UltimateWeaponRepository.kt   # Ultimate weapon interface (R4-06: upgradePathLevel replaces upgradeWeapon)
domain/repository/StepRepository.kt             # Daily step records + escrow + Health Connect methods. #227: + sumCreditedSteps / creditBattleStepsAtomic / creditBossPowerStonesAtomic (the battle/boss atomic credit moved off DailyStepDao behind this port)
domain/repository/WalkingEncounterRepository.kt # Walking encounter interface
domain/repository/MissionRepository.kt          # (#227/#219) daily-mission port: getMissionsForDate / generateForDate / markClaimed / updateProgress + reactive presentation reads observeMissionsForDate(Flow) / observeClaimableCount(Flow) (#219 moved these off the raw DAO)
domain/repository/MilestoneRepository.kt        # (#227/#219) milestone port: getClaimedMilestoneIds + claimMilestoneAtomic + observeClaimedMilestoneIds(): Flow<Set<String>> (#219 — Home/Missions claimed-set read)
domain/repository/DailyLoginRepository.kt       # (#227) daily-login port: getByDate / upsert (DailyLogin)
domain/repository/WeeklyChallengeRepository.kt  # (#227/#219) weekly-challenge port: getByWeek / upsert + getLastNWeeks(limit): List<WeeklyChallenge> (#219 — Economy dashboard history, was a direct DAO read)
domain/repository/BillingManager.kt             # Billing interface (purchase, query, reconcilePendingPurchases with default no-op so fakes inherit do-nothing contract; C.5 PR 1). Plan 31 PR B added `getPriceDisplay(product): String?` (default null) so the Store screen can read live formatted prices from Play Billing's ProductDetails.priceDisplay instead of the static BillingProduct.priceDisplay constants.
domain/repository/RewardAdManager.kt            # Reward ad interface (show ad, availability)
domain/repository/CosmeticRepository.kt         # Cosmetic store interface + `idExists(cosmeticId): Boolean` (C.4 — used by ClaimMilestone to pre-flight MilestoneReward.Cosmetic ids and surface UnknownCosmetic result variant for the 3 currently-mismatched milestone cosmetic ids)
domain/usecase/CalculateUpgradeCost.kt          # Cost formula: baseCost * scaling^level
domain/usecase/CanAffordUpgrade.kt              # Affordability check against wallet
domain/usecase/PurchaseUpgrade.kt               # Delegates to WorkshopRepository.purchaseUpgradeAtomic — atomic deduct + level-set (B.2 PR 1)
domain/usecase/QuickInvest.kt                   # Recommends cheapest affordable upgrade
domain/usecase/ResolveStats.kt                  # Workshop + in-round + lab research levels → ResolvedStats. RO-08: in-round multiplier `ir(...)` extended to all 14 stat-bearing upgrade types (was previously only DAMAGE/ATTACK_SPEED/HEALTH); multiplicative stats follow `(1+ws*x)*(1+ir*x)`, additive stats sum levels before per-level effect and any cap. Range now multiplicative + clamped to BASE × 3. RO-11 #A.1: optional `labLevels: Map<ResearchType, Int> = emptyMap()` parameter adds 3rd multiplicative tier — DAMAGE_RESEARCH (+5 %/lvl), HEALTH_RESEARCH (+5 %/lvl), CRITICAL_RESEARCH (+3 %/lvl), REGEN_RESEARCH (+4 %/lvl). Default empty preserves existing call sites.
domain/usecase/CalculateDamage.kt               # Raw damage + crit roll + damage/meter bonus → DamageResult
domain/usecase/CalculateDefense.kt              # Damage reduction (cap 75%) + flat block
domain/usecase/UpdateBestWave.kt                # Compares wave to stored best, persists if new record
domain/usecase/CheckTierUnlock.kt               # Checks wave milestones for tier unlock eligibility
domain/usecase/UnlockUltimateWeapon.kt           # Checks Power Stone balance, deducts, sets isUnlocked flag (R4-06)
domain/usecase/UpgradeUltimateWeapon.kt          # Per-path UW upgrade: takes UWPath param, cost scaling per level, max level 10 (R4-06)
domain/usecase/CalculateResearchCost.kt          # Research cost: baseCostSteps × costScaling^level
domain/usecase/CalculateResearchTime.kt          # Research time: baseTimeHours × timeScaling^level
domain/usecase/StartResearch.kt                  # Validates slots/affordability/max level, deducts Steps, starts timer
domain/usecase/CompleteResearch.kt               # Completes research when timer elapsed, increments level
domain/usecase/RushResearch.kt                   # Instant complete via Gems (50–200 linear cost)
domain/usecase/UnlockLabSlot.kt                  # Unlock lab slot (200 Gems, max 4)
domain/usecase/CheckResearchCompletion.kt        # Auto-completes all expired research on app launch. #211 (ADR-0036): `invoke(now)` documents that callers pass the TRUSTED current time (the TimeIntegrity `trustedWallClock` capped-accrual anchor), NOT raw wall-clock, so an in-session forward clock jump can't instantly finish in-flight research. Default arg stays `System.currentTimeMillis()` for non-#211 callers.
domain/usecase/UpdateCompleteResearchMissionProgress.kt # R3-03 (#1): COMPLETE_RESEARCH daily-mission tick. Encapsulates the DAO lookup + progress update + count gating in one place; takes `DailyMissionDao`, exposes `invoke(completedCount: Int, today: String = LocalDate.now().toString())`. Early returns on `completedCount <= 0` (closes the false-trigger). Additive-with-cap progress increment so a multi-completion auto-batch on app launch correctly reflects all completions while clamping at `m.target`. Idempotent: missing row / already-claimed / already-completed / DAO exceptions are silent no-ops (matches the prior `LabsViewModel.updateResearchMission` fail-open contract).
domain/usecase/OpenCardPack.kt                   # Opens card pack: 3 tiers, rarity rolling, duplicate→copyCount increment (R4-08, ADR-0010)
domain/usecase/UpgradeCard.kt                    # Card upgrade: spends copies (copiesPerLevel by rarity: 3/4/5) via decrementCopiesAndLevelUp; max level 7 (R4-08, ADR-0010)
domain/usecase/ApplyCardEffects.kt               # Post-process ResolvedStats with equipped card effects
domain/usecase/ManageCardLoadout.kt              # Equip/unequip cards (max 3 loadout)
domain/usecase/GenerateSupplyDrop.kt             # Seeded random supply drop generation (4 triggers)
domain/usecase/ClaimSupplyDrop.kt                # Credits reward to player, marks drop claimed. #122: mark-first via the atomic guarded claimDrop (returns Boolean); credits only when the claim transitions unclaimed→claimed, so a double-tap credits exactly once (ADR-0020)
domain/usecase/ClaimMission.kt                   # #122 (ADR-0020): atomic daily-mission claim extracted from MissionsViewModel. Mark-first via guarded DailyMissionDao.markClaimed (AND claimed=0, returns Int); credits Gems/PowerStones only on rows==1. Returns ClaimMissionResult (Success | NotClaimable). Closes the double-tap double-credit (audit #10)
domain/usecase/TrackWeeklyChallenge.kt           # Weekly step challenge PS awards (50k/75k/100k)
domain/usecase/TrackDailyLogin.kt                # Daily login PS + Gem streak. #211 (ADR-0036): `checkAndAward(..., isRollback = false)` — on `isRollback = true` (TimeIntegrity Rollback verdict, backward clock-tamper) it refuses ALL credit for the tampered date and writes nothing (no streak/gem/season-bonus advance, no DailyLogin row, no gemsClaimed latch).
domain/usecase/AwardWaveMilestone.kt             # PS on new personal-best waves (1/2/5)
domain/usecase/AwardBossPowerStones.kt           # PS on boss kills: tier×PS (T1=1, T10=10), 100/day cap, atomic DAO path (R4-07 / ADR-0009)
domain/usecase/AwardBattleSteps.kt               # Credits per-kill battle Steps to the wallet under the per-day DAILY_BATTLE_STEP_CAP (separate from the 50k walking ceiling); cap-check + counter-increment + wallet-credit run in one Room @Transaction (creditBattleStepsAtomic, RO-02); returns amount actually credited
domain/usecase/CheckMilestones.kt                # Detect newly achievable walking milestones
domain/usecase/ClaimMilestone.kt                 # Credit milestone rewards via MilestoneDao.claimMilestoneAtomic (gems + power stones) with pre-flight CosmeticRepository.idExists check; returns `ClaimMilestoneResult` sealed type (Success | InsufficientSteps | AlreadyClaimed | UnknownCosmetic(cosmeticId)) so the 3 mismatched milestone cosmetic ids surface explicitly instead of silently dropping (C.4)
domain/usecase/GenerateDailyMissions.kt          # Generate 3 daily missions (date-seeded random). #127: builds the deterministic batch then delegates to DailyMissionDao.generateForDate (the (date,missionType) unique index + IGNORE is what makes concurrent VM-init re-generation safe).
domain/usecase/PurchaseGemPack.kt                # Purchase Gem pack via BillingManager
domain/usecase/CombatPower.kt                    # (#29) Combat-power index — steady-state single-target DPS *proxy* `damage × attackSpeed × (1 + critChance × (critMultiplier−1))` used ONLY to rank Workshop upgrades + drive the value indicator. Returns a bare Double; type-incompatible with the engine's ResolvedStats stat-sinks, so it can NEVER feed the simulation (spec INV-3). Pure Kotlin.
domain/usecase/WorkshopLevels.kt                 # (#29) Shared workshop-dimension level helpers (object): levelOf / isAtMax (gates on the config maxLevel) / withIncremented (bump the WORKSHOP dimension by one, no clamp). Both DescribeUpgradeEffect.workshopPreview + EvaluateUpgradeValue route through these so the Now→Next string and the value delta can't diverge (spec §5.1 / INV-6). Pure Kotlin.
domain/usecase/EvaluateUpgradeValue.kt           # (#29) Value-per-step ranking + Best-Buy selection. Candidate only when buying one workshop level raises CombatPower (Δpower>0 — excludes Range / Damage-per-meter / Rapid Fire / all Defense+Utility, and Critical Factor while crit chance is 0); maxed + cost-0 excluded. Best Buy = highest valuePerStep affordable, else highest-value overall flagged bestBuyAffordable=false ("save up"); ties break by UpgradeType ordinal so the badge can't flicker. Emits the `UpgradeValue` data class (type, valuePerStep, percentPerKSteps, barFraction 0f..1f, isBestBuy, bestBuyAffordable). Increments the WORKSHOP dimension via WorkshopLevels. Pure Kotlin.
domain/usecase/DescribeUpgradeEffect.kt          # RO-11 #C / RO-10: per-row "Now → Next" readout for in-round upgrade menu. UpgradeEffectReadout(current, next?) data class; next is null at maxLevel. Stat-bearing upgrades call ResolveStats twice (current + post-purchase) and format the relevant ResolvedStats field with unit suffix (dmg/HP/px//s/%); cash utilities + hidden-from-in-round upgrades (STEP_MULTIPLIER, RECOVERY_PACKAGES, included for Workshop reuse) compute from UpgradeConfig.effectPerLevel directly with cap clamps. Format strings pinned to Locale.ROOT for deterministic output across locales (English-only v1.0 strings; localization deferred to v2.0). RO-12: optional `equippedCards: List<OwnedCard> = emptyList()` invoke param + `applyCardEffects: ApplyCardEffects = ApplyCardEffects()` constructor param threads card effects post-resolveStats so the readout matches the live engine pipeline (`resolveStats → applyCardEffects → engine.setStats`). HEALTH_REGEN format bumped from %.1f/s to %.2f/s so +2 %/level on a base ~1.3/s produces a visibly different Lv 0 → Lv 1 readout (was "1.3/s → 1.3/s" pre-RO-12). #29: added `workshopPreview(workshopLevels, type, labLevels, equippedCards)` — a WORKSHOP-dimension Now→Next (in-round dimension intentionally empty); reuses the private `format` formatter + the shared `WorkshopLevels` cap-test/increment so this readout and EvaluateUpgradeValue's value delta can't diverge (spec INV-6).
domain/battle/entity/EntityProtocol.kt # V1X-09 Phase 3 (ADR-0012, slices 3–4). Pure-domain entity seam (no Android imports): `val isAlive` + `val x` / `val y` / `val width` (positional reads for the collision sweep, slice 4) + `val isChronoSlowable` (default false) + `fun update(deltaTime)`. Lets `Simulation.tickEntities` + the `Simulation` collision sweeps iterate battle entities without touching Canvas. Implemented by presentation `Entity`; `EnemyEntity` overrides `isChronoSlowable = true`.
domain/battle/entity/ProjectileState.kt # V1X-09 Phase 2 (ADR-0012, first per-entity extraction). Pure projectile-motion state (no Android imports): owns x/y + alive flag + homing-toward-target step. Presentation ProjectileEntity delegates update() here. Establishes the domain/battle/entity/<Name>State pattern for the remaining entities.
domain/battle/entity/OrbState.kt # V1X-09 Phase 2 (ADR-0012). Pure orbit position + radial-oscillation state (no Android imports): owns angle + radialPhase + derived (x,y) + currentOrbitRadius; companion consts ORBIT_RADIUS_MIN/MAX (25/70 px) + ORBIT_PERIOD_SEC (2.5 s) + MID/AMPLITUDE/RADIAL_ANGULAR_SPEED (the #54 oscillation math). Presentation OrbEntity delegates position here; enemy-proximity / hit-cooldown stays in OrbEntity.
domain/battle/entity/EnemyState.kt # V1X-09 Phase 2 (ADR-0012). Pure movement + attack-cooldown state (no Android imports): owns (x,y) + attackCooldown + initialDist; spawn(x,y) captures initialDist, update(dt):Boolean does the homing step while beyond stopDistance (RANGED uses initialDist*RANGED_STOP_FACTOR 0.4, melee uses MELEE_RANGE 40) and returns true on the frame an attack should fire, applyKnockback shifts position. Presentation EnemyEntity delegates motion/attack here + syncs x/y back to the Entity base each tick; HP/armor/death/attack-callbacks/render stay in EnemyEntity.
domain/battle/entity/ZigguratState.kt # V1X-09 Phase 2 (ADR-0012, final entity). Pure ziggurat simulation state (no Android imports): owns stats (private set) + currentHp/maxHp + @Volatile rapidFireMultiplier + private attackCooldown; derived attackRange (=stats.range) + attackInterval (=1/(attackSpeed*rapidFireMultiplier)); regenHp(dt) clamps at maxHp; tickAttackReady(dt):Boolean decrements cooldown returns ≤0; onFired() resets to attackInterval; holdReady() clamps to 0; updateStats(newStats) redirects derived reads. Presentation ZigguratEntity delegates its entire public surface here so GameEngine/BattleViewModel are untouched.
```

## Presentation Layer

```
presentation/MainActivity.kt                      # Single Activity, Scaffold + NavHost + BottomNavBar, permissions; onResume/onPause drive ActivityProvider.set/clear for Play Billing purchase flow (C.5 PR 2); onResume also fires a flag-gated one-shot UMP consent prefetch via injected ConsentManager so the first reward-ad tap doesn't pay the init latency (C.6 PR 2); first-launch onboarding (#24): startDestination = Screen.startDestination(synchronous OnboardingPreferences read), ONLY the cold-permission request branch gated behind onboardingComplete, deep-link collector gated on live nav state, permanent-denial recovery via Snackbar→ACTION_APPLICATION_DETAILS_SETTINGS in the launcher callback. #190 REL-1 / #374: injects CrashBreadcrumbStore + a one-shot `LaunchedEffect(Unit)` that peeks the breadcrumb → shows a crash-notice snackbar WITH a "Report" action (hoisted `stringResource` to satisfy Compose lint); on ActionPerformed it fires the top-level `buildCrashReportIntent(...)` (ACTION_SENDTO mailto, resolve-guarded — needs the manifest `<queries>` SENDTO/mailto entry) → `clear()` still after the awaited snackbar so a Report tap emails first. `buildCrashReportIntent` is extracted top-level (mirrors `openPrivacyPolicy`) so it is unit-testable off the Activity (CrashReportIntentTest). #250/ADR-0028: onResume also calls `service.reconcileBillingSafely(billingManager)` on activityScope/IO — foreground reconcile of pending/offline Play Billing purchases every resume (StepSyncWorker is the background safety net). #261/ADR-0029: top-level `requestBatteryExemption(context)` fires ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS (package: Uri; falls back to ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS), wired to the onboarding `onRequestBatteryExemption` callback + the Settings `onOptimizeBattery` row.
presentation/HealthConnectPermissionActivity.kt    # Scrollable in-app privacy rationale for Health Connect permissions (the system VIEW_PERMISSION_USAGE / ACTION_SHOW_PERMISSIONS_RATIONALE target). #192: present-tense + accurate — removed the false "never uploaded/never shared" claim, added the AdMob/advertising-ID disclosure + hosted-policy URL, unified contact email to jonwhitefang@gmail.com.
presentation/navigation/Screen.kt                 # Sealed class: 13 deep-linkable routes (Home, Workshop, Battle, Labs, Stats, Weapons, Cards, Supplies, Economy, Missions, Settings, Store, Help) + a 14th first-run/replay-only Onboarding route (deliberately excluded from items/allScreens/argumentFreeRoutes so it's not a public deep-link target); pure startDestination(hasCompletedOnboarding) helper; pure secondaryTitle(route) helper → @StringRes Int? bar-title resource for the 8 push-nav secondary screens, null elsewhere, resolved with stringResource at the MainActivity call site (i18n #34, ADR-0014 phase 2; drives SobTopAppBar gating, #161). i18n #34 phase 3 (B): the bottom-nav `label` ctor param is now `@StringRes labelRes: Int` (was a String), resolved via stringResource in BottomNavBar; the `by lazy` route lists / fromRoute / startDestination / secondaryTitle are untouched (#161, DeepLinkRoutingTest green).
presentation/navigation/BottomNavBar.kt            # Bottom navigation bar with 5 items. Tab tap → tab root via the shared NavOptionsBuilder.bottomNavOptions() (popUpTo(Home)+launchSingleTop, NO save/restore — flat graph; ADR-0023, #161). bottomNavOptions() is reused by BottomNavRestoreTest so the guard drives the exact NavOptions.
presentation/home/HomeViewModel.kt                 # @HiltViewModel: combines profile + step flows → HomeUiState. #55 fix: `init` captures the `List<ResearchType>` returned by `CheckResearchCompletion(labRepository)()` and passes `completed.size` to a new `private val updateMissionProgress = UpdateCompleteResearchMissionProgress(dailyMissionDao)` field, mirroring the LabsViewModel R3-03 pattern. Pre-fix the return value was discarded so background-completed research never advanced the COMPLETE_RESEARCH daily mission. `dailyMissionDao` was already injected for the existing `countClaimable` consumer; no Hilt graph change. #211 (ADR-0036): read-only time-axis consumer — `init` evaluates `TimeIntegrity.evaluate(timeBaselineSource.read…/current…)` to derive `trustedNow = verdict.newBaseline.trustedWallClock` (passed to `CheckResearchCompletion(now = trustedNow)`) + `isRollback = verdict is TimeVerdict.Rollback`; never persists the baseline (DailyStepManager owns it).
presentation/home/HomeUiState.kt                   # UI state: steps, balance, tier, biome, bestWave. #224: pure `showFirstWalkPrompt` predicate (todaySteps==0 && stepBalance<FIRST_WALK_BALANCE_THRESHOLD=100, suppressed while loading/error) gates the Home first-walk EmptyState teaching prompt; guarded by HomeFirstWalkPromptTest
presentation/home/HomeScreen.kt                    # Dashboard: step card, currencies, tier selector, battle button
presentation/home/TierSelector.kt                  # Horizontal tier chip row with lock/unlock states
presentation/workshop/WorkshopViewModel.kt         # @HiltViewModel: upgrades + wallet → WorkshopUiState. #29: wires the decision-support use cases — populates each `UpgradeDisplayInfo.nowNext` via DescribeUpgradeEffect.workshopPreview and `value` via EvaluateUpgradeValue (Best Buy is selected PER TAB/category, not globally). #234: takes SavedStateHandle; selected tab is savedStateHandle.getStateFlow("selectedCategory", ATTACK) as a combine source (selectCategory writes it back) → survives process death.
presentation/workshop/WorkshopUiState.kt           # UI state: upgrade list, balance, selected category. #29: `UpgradeDisplayInfo` gains `nowNext: UpgradeEffectReadout?` (workshop-dimension Now→Next, null only if uncomputable) + `value: UpgradeValue?` (combat-power value/Best-Buy data; null for non-combat upgrades, i.e. Δpower ≤ 0).
presentation/workshop/WorkshopScreen.kt            # 3-tab layout, upgrade list. V1X-13: weapons/cards button labels + balance header via stringResource (workshop_*).
presentation/workshop/UpgradeValueLabel.kt         # (#29) Pure `formatPowerPerKStepsLabel(percentPerKSteps)` → the value-bar label "+X.X% power / 1,000 steps". `Locale.ROOT` decimal separator (matches DescribeUpgradeEffect.fmt, spec INV-5 — do NOT copy WorkshopViewModel.statValueFor's default-locale format) + a +0.1% floor so a card that legitimately carries a bar never shows a contradictory "+0.0%".
presentation/workshop/UpgradeCard.kt               # Reusable upgrade card (affordable/expensive/maxed states). V1X-13: MAX/level/cost via stringResource (upgrade_max/upgrade_level/upgrade_cost_steps). #154: the Card is `enabled = canAfford && !isMaxed` (genuinely un-clickable at cap, not just a no-op onClick) with `disabledContainerColor == containerColor` so disabling keeps the Gold MAX tint / normal surface (dim owned by the `alpha` modifier). Bundle C (#162): consumes the shared PurchasePulse (rememberPulse/pulseScale, 1.12×) + fires rememberHaptics().tap() on a successful spend (inline 1.05× pulse removed). #29: renders the decision-support affordances — Now→Next stat preview (from `nowNext`), the combat-power "value per step" indicator + bar (from `value`, via `formatPowerPerKStepsLabel`), and the single "★ BEST BUY" badge (affordable-first, greyed "save up" fallback when `bestBuyAffordable` is false).
presentation/battle/BattleScreen.kt                # Compose wrapper: AndroidView + overlays (HUD, pause, post-round, #190 battle-error), auto-pause. #233/ADR-0029: a DisposableEffect + LocalActivity locks requestedOrientation=PORTRAIT on enter / UNSPECIFIED on dispose, so a mid-round rotation can't recreate+desync the engine/VM (entry-time recreate is harmless — round starts after configure). #190 REL-2: `showGameChrome = roundActive && !state.battleError` gates all 5 interactive chrome sites (quit / UW bar / rail / upgrade menu / pause); `BattleErrorOverlay` rendered as the last child before SnackbarHost (transparent scrims don't block touches, so chrome must be suppressed, not just z-ordered). #171 (PR #177): the old horizontal-scrolling bottom-center control Row was REMOVED — speed (1x/2x/4x) + Pause + Upgrade-toggle now live on the left vertical `BattleControlRail` (`Alignment.CenterStart`, shared `railStartInset` = systemBars∪displayCutout Start-only); the `UltimateWeaponBar` owns bottom-center alone (`Alignment.BottomCenter` + `navigationBars` inset + 24dp); the in-round upgrade menu spans full screen width at `BottomCenter` and clears the rail VERTICALLY (its fixed `IN_ROUND_MENU_HEIGHT` sits its top below the rail's bottom, so the rail stays tappable while shopping). No more independent `padding(bottom=…)` offsets contending for the bottom edge (that three-way contention was the #171 overlap bug). The R3-04 horizontalScroll safety net is gone with the row. V1X-13: HUD (wave header / cash / Battle-Steps banner) + speed labels and the quit/speed/pause/upgrades content descriptions via stringResource (battle_*, cash_amount, steps_earned_banner); ▶/⏸ glyphs left literal. #214: a polite TalkBack live region — an invisible `size(1.dp).alpha(0f)` sibling node with `liveRegion=Polite` + contentDescription fed from uiState; a LaunchedEffect diffs the prev→next BattleSnapshot via the pure `battleAnnouncement` and a @Composable `when` maps the sealed result to a localized string (announces wave/phase/25%-health/round-over/error transitions; canvas itself stays pixels).
presentation/battle/BattleViewModel.kt             # @HiltViewModel: 15-param constructor (RO-11 added LabRepository); round lifecycle in `runEndRoundPersistence` wrapped in `AppDatabase.withTransaction` (B.2 PR 5) with per-write runCatching error isolation (B.3 PR 1); `onCleared` guard launches persistence on `@ApplicationScope CoroutineScope` when round is mid-progress (B.3 PR 2); hydrates `engine.cosmeticOverrides` from CosmeticRepository (C.2 PR 1); RO-11 #A.2 + #B.1: `var labLevels: Map<ResearchType, Int>` and `var startWave: Int` snapshots populated in init/playAgain; private helpers `cashResearchMultiplier()` / `uwCooldownMultiplier()` / `waveSkipStartWave()`; pushes engine multipliers via `cashResearchMultiplier` + `uwCooldownMultiplier` and threads `startWave` through `surfaceView.configure(...)`; RO-11 #C: public `describeEffect(type)` exposes `DescribeUpgradeEffect` use case sharing the same `resolveStats` instance for drift-free Now → Next preview. RO-12: private `resolveCurrentStats(inRound)` helper runs the live-engine pipeline (`resolveStats(workshop, inRound, lab) → applyCardEffects(stats, equippedCards).stats`); used by `purchaseInRoundUpgrade` so lab + card multipliers survive every in-round purchase (pre-RO-12 the call site dropped both `labLevels` and `applyCardEffects`, silently stripping research + card bonuses for the rest of the round); `describeEffect` threads `equippedCards` through to DescribeUpgradeEffect to keep the readout in lockstep. V1X-15b: `@VisibleForTesting internal applyResearchParams(engine)` dedups the cash/UW-cooldown/ENEMY_INTEL engine pushes across `startPollingEngine` + `playAgain`; private `enemyIntelLevel()` reads `labLevels[ENEMY_INTEL]` and is pushed to `engine.enemyIntelLevel`. #146: the polling-loop `enemyCount` now reads `eng.aliveEnemyCount()` (authoritative, derived live) instead of the removed `spawner?.enemiesAlive` tally. #190 REL-2: `startPollingEngine` wires `surfaceView.onLoopError = { onBattleLoopError(it) }`; the poll `while(true)` breaks on `_uiState.value.battleError`; `roundEnded` is now `@Volatile` (loop-thread write / main-thread read); `@VisibleForTesting onBattleLoopError(t)` sets battleError=true + roundEnded=true (so the poll stops AND onCleared skips persistence of the corrupt round) and deliberately does NOT set eng.roundOver. #191 CONC-2: the UW-slot poll read uses `eng.uwSnapshot()` (not `eng.uwStates`).
presentation/battle/BattleUiState.kt               # UI state: wave, HP, cash, speed, pause, RoundEndState. #190 REL-2: +`battleError: Boolean` (set when the loop thread crashed → drives BattleErrorOverlay + suppresses round chrome).
presentation/battle/BattleAnnouncer.kt             # #214: pure (no Android/Compose) decision layer for the battle TalkBack live region. `BattleSnapshot` (6 uiState primitives) + sealed `BattleAnnouncement` (Wave/Phase/Health/RoundOver/Error) + `battleAnnouncement(prev,next): BattleAnnouncement?` (priority error>roundOver>wave>phase>health; null pre-round when phase blank/maxHp<=0) + `healthBucket` (quarter brackets, guards maxHp<=0). Composable owns localization. JVM-tested by BattleAnnouncerTest.
presentation/battle/GameSurfaceView.kt             # SurfaceView + SurfaceHolder.Callback, manages game loop thread; tracks `currentStartWave` and threads it through all 3 `engine.init` call sites (configure / surfaceCreated / surfaceChanged) so `playAgain` mid-session sees the latest WAVE_SKIP level (RO-11 #B.1); R3-01: surfaceCreated + surfaceChanged route through `@VisibleForTesting internal fun initEngineIfNeeded()` which gates `engine.init` on `!engine.hasWaveProgress()` so a background-and-resume cycle preserves the in-flight round; `@Volatile internal var pendingSpeed` / `pendingPaused` captured by `setSpeedMultiplier` / `setPaused` survive the `gameThread = null` lifecycle gap and seed the new thread when surfaceCreated fires. #190 REL-2: builds `CrashBreadcrumbStore(context)` directly (same prefs file as the Hilt singleton) and passes it to the 3-arg GameLoopThread ctor; `@Volatile var onLoopError` whose setter forwards to the live thread AND is re-seeded onto each new thread in surfaceCreated (load-bearing — the VM sets it once). #245: `soundManager` is a recreatable var — `releaseSoundManager()` (surfaceDestroyed) releases the SoundPool + nulls engine.soundManager (clean no-op in the destroyed window); `ensureSoundManager()` (surfaceCreated) rebuilds from sound_prefs + re-points the engine (idempotent via a `soundManagerReleased` flag) so battle SFX survive a background→resume.
presentation/battle/GameLoopThread.kt              # Dedicated thread: fixed timestep (60 UPS), accumulator, speed multiplier. #126: accumulator clamped via SimulationMath.clampAccumulator (bounds catch-up backlog, anti-spiral-of-death) after the speed-scaled accumulate. #190 REL-2: per-tick update()/render() wrapped in try/catch → record breadcrumb (CrashBreadcrumbStore ctor param) → isRunning=false → onLoopError?.invoke (all swallowed; inner lockCanvas/unlock try/finally stays nested so a render crash unlocks first); @Volatile onLoopError forwarded/re-seeded by GameSurfaceView.
presentation/battle/engine/GameEngine.kt           # Central coordinator: entity list, update/render dispatch, wave/collision integration; +hasWaveProgress() (B.3 PR 2); +@Volatile cosmeticOverrides: Map<CosmeticCategory, CosmeticItem> consulted in init() to select ziggurat layer colors (C.2 PR 1); +applyStats() single-mutation point that propagates ResolvedStats updates to engine + ziggurat in lock-step (RO-08); +updateEffectiveLevels() public setter for in-round cash-utility levels (RO-08); +tickRecoveryPackages() periodic-heal pulse (RO-08, RECOVERY_PACKAGES — 30s interval, 1% per level capped at 50%, SPAWNING-phase only); RO-09 #1: entities.forEach scales `deltaTime` for `EnemyEntity` only when `chronoActive` true (`CHRONO_SLOW_FACTOR=0.10f` companion constant) so CHRONO_FIELD UW actually slows enemies; projectiles/orbs/ziggurat keep unscaled `deltaTime`. R4-01: Step Overdrive deleted entirely — `activeOverdrive` / `overdriveTimeRemaining` / `preOverdriveStats` / `overdriveAuraEffect` fields removed; `activateOverdrive` / `expireOverdrive` methods deleted; the `update()` overdrive-timer block deleted. GOLDEN_ZIGGURAT becomes the sole writer of `fortuneMultiplier`: activate hard-sets to 5.0×, expiry resets to 1.0× (collapses the 3-site coerceAtLeast/cross-overdrive RO-09 #2 lifecycle to 2 unconditional writes). R3-02: 2 onMeleeHit lambdas (initial WaveSpawner wiring + SCATTER child enemy spawn) flipped from `{ _, dmg -> applyDamageToZiggurat(dmg, null) }` to `{ atk, dmg -> applyDamageToZiggurat(dmg, atk) }` so `applyThorn` actually fires; +`lifestealAccumulator: Double = 0.0` field reset in init() + new `applyLifesteal(healAmount)` helper that mirrors applyThorn's shape and emits `FloatingText("+X HP", STEP_COLOR)` each time the accumulator crosses an integer HP threshold so low-level LIFESTEAL produces visible burst feedback (math identical to pre-fix; only the visual is new). R4-06: UWState 4-field struct (type + damageLevel + secondaryLevel + cooldownLevel); auto-trigger on cooldown replaces manual activation; per-path `activateUW`; `chronoSlowFactor` instance field (companion `CHRONO_SLOW_FACTOR` constant removed). V1X-15b: +@Volatile `enemyIntelLevel` (set by BattleViewModel, not reset in init) gating 3 overlays — `nextWaveCompositionLabel()` (L1+, fed into `WaveCooldownText`) / `bossCountdownLabel()` (L10) / L5+ per-enemy HP-% text drawn in `render()`. #118: +private `entitiesLock` monitor — `update()` (whole tick), `init()` (whole rebuild), `applyStats` orb-reconcile branch, and `render()` (snapshot-under-lock then draw outside) all `synchronized(entitiesLock)` so the loop thread can't collide with a main-thread ORBS reconcile / playAgain re-init; `stats` marked `@Volatile`. Guarded by `GameEngineConcurrencyTest`. #119: GOLDEN modelled as a re-derived damage layer — +`goldenDamageMult` field (captured at activation, reset on expiry/init); `updateZigguratStats` re-captures `preGoldenStats` to the new base + re-applies the multiplier while GOLDEN active, so an in-round upgrade bought mid-GOLDEN survives expiry instead of being rolled back to the stale snapshot. #146: +public `aliveEnemyCount()` = `synchronized(entitiesLock) { entities.count { it is EnemyEntity && it.isAlive } }` — authoritative HUD enemy count derived live from the entity list (read by BattleViewModel), replacing the removed `WaveSpawner.enemiesAlive` tally + its `onEnemyKilled()` decrement in `handleEnemyDeath`; immune to the negative drift (SCATTER children bypassed the tally's only increment; `onDeath` re-fires double-decremented). #26 A28: collision scratch buffers (pre-typed mutable lists populated under `entitiesLock` before calling CollisionSystem) eliminate per-frame filterIsInstance allocations on the hot game-loop path; partition now happens once inside `update()` while the lock is held and the typed snapshots are passed to CollisionSystem.checkCollisions. #26 A31: `chronoOverlayPaint` cached as a field (allocated once in init, not per-frame in render) to eliminate the per-render Paint allocation for the CHRONO_FIELD slow overlay. +`@VisibleForTesting fun setChronoActiveForTest(active: Boolean)` public seam so ChronoOverlayPaintTest can verify the cached-paint identity invariant without a full game loop. #191 CONC-2: `initUWs` body wrapped in `synchronized(entitiesLock)` (main-thread replay vs the loop-thread `updateUWs` iteration); +`uwSnapshot(): List<UWState> = synchronized(entitiesLock) { uwStates.toList() }` for the 200ms VM poll read. Guarded by the new `GameEngineConcurrencyTest` replay-race test. #243: the per-tick projectile-trail spawn loop now time-throttles via `advanceTrail(e.trailTimer, deltaTime)` (loop-thread-only, under entitiesLock, !reducedMotion-gated) — emits at most one trail per TRAIL_INTERVAL of sim-time, so the 4× catch-up no longer floods the 200-slot ParticlePool. Loop unchanged in placement/locking; not merged with the A28 partition pass. **#230/#231 (ADR-0012 Phase 4):** decomposed 1233→618 LOC into an orchestrator + sole-`entitiesLock`-owner + façade that `implements UWHost, BuffHost, CombatHost` and composes four collaborators (BattleRenderer/UWController/BuffTickers/CombatResolver, below). Moved out: `render()` body + 3 Paint fields → BattleRenderer; UW lifecycle (`updateUWs`→`uwController.update`, `activateUW`, `initUWs`, `uwSnapshot`, `resetUWCooldowns`, UWState, chronoActive/chronoSlowFactor/golden*/fortuneMultiplier) → UWController; tickRecoveryPackages(→tickRecovery)/tickRapidFire/applyLifesteal + their timer fields → BuffTickers; onProjectileHitEnemy/onOrbHitEnemy/applyDamageToZiggurat/applyThorn/handleEnemyDeath/handleWaveComplete + calculateDamage/Defense → CombatResolver; cash formulas + companion constants → SimulationMath. Private `tier`/`conditions` fields renamed `currentTier`/`battleConditions` (avoid the `init(playerTier)` param clash); `secondWindUsed` write-back exposed as `consumeSecondWind()`; `updateZigguratStats` delegates the #119 re-layer to `uwController.relayerBaseStats`. KEPT on engine: entities/pendingAdd/entitiesLock, init, update tick assembly, applyStats, spawnOrbs, addEntity, aliveEnemyCount, getAliveEnemies/findNearestEnemies (exposed as host aliveEnemies()/nearestEnemies()), wave-announcement methods, the @Volatile VM-written fields, the façade getters. Thread-safety unchanged: GameEngineConcurrencyTest/EffectEngineConcurrencyTest pass UNCHANGED. +3 @VisibleForTesting collaborator getters for the GameEngineTest reflection re-points.
presentation/battle/engine/BattleHosts.kt          # #230/#231 (ADR-0012 Phase 4). Three narrow capability interfaces GameEngine implements + the collaborators depend on: UWHost (ziggurat/screen dims/reducedMotion/uwCooldownMultiplier/effectEngine/soundManager/currentStats/applyStats/aliveEnemies), BuffHost (ziggurat/wavePhase/effectEngine/strings/wsLevel), CombatHost (ziggurat/currentStats/conditions/tier/effectEngine/soundManager/strings/reducedMotion/simulation/fortuneMultiplier/cashResearchMultiplier/cashBonusPercent/secondWindHpPercent/consumeSecondWind/addPending/aliveEnemies/nearestEnemies/wsLevel/applyLifesteal). Threading contract documented: all members UNSYNCHRONIZED, invoked only inside the engine's held entitiesLock; aliveEnemies() carries the #125 no-cache hot-path contract.
presentation/battle/engine/BattleRenderer.kt       # #231 (ADR-0012 Phase 4). Owns ALL Canvas Paint (chronoOverlayPaint/hpPercentPaint/bossCountdownPaint — A31 cached) + the per-frame draw sequence lifted verbatim from GameEngine.render(): background, screen-shake apply, entity snapshot draw, effects, chrono overlay, screen-shake restore, ENEMY_INTEL L5+ HP-% labels, health bar, ENEMY_INTEL L10 boss countdown. Pure draw — receives the under-lock entity snapshot + per-frame scalars as params; mutates no shared state. GameEngine now holds zero Paint fields.
presentation/battle/engine/UWController.kt         # #231 (ADR-0012 Phase 4). UW lifecycle state machine lifted verbatim from GameEngine: UWState (4-field per-path struct) + uwStates + chronoActive/chronoSlowFactor (var private set) + goldenZigActive/preGoldenStats/goldenDamageMult + fortuneMultiplier (var private set, read by CombatResolver cash formulas via CombatHost). Methods: resetRoundState (init-path reset, called under engine lock), relayerBaseStats (#119 GOLDEN re-layer — main-thread lock-free path, returns the stats the engine should apply), initUWs/uwSnapshot/resetUWCooldowns, activateUW (per-path DEATH_WAVE/CHAIN_LIGHTNING/BLACK_HOLE/CHRONO/POISON/GOLDEN), update (was updateUWs — advanceUWTimers via the engine-owned Simulation passed by ctor; expiry + ongoing BLACK_HOLE/POISON effects; auto-trigger). Reaches engine state via UWHost; holds no monitor (runs inside the engine's held entitiesLock / the wrapped main-thread initUWs/uwSnapshot paths, #191). +setChronoActiveForTest seam (delegated from GameEngine).
presentation/battle/engine/BuffTickers.kt          # #231 (ADR-0012 Phase 4). Per-mechanic in-round buff timers lifted verbatim from GameEngine: recoveryTimer/rapidFireTimer/rapidFireActiveRemaining/lifestealAccumulator + reset() + tickRecovery (RO-08 RECOVERY_PACKAGES, 30s interval, SPAWNING-phase only) + tickRapidFire (R4-03, active-decrement-before-fire ordering for the L10 seamless transition) + applyLifesteal (R3-02 accumulator + FloatingText feedback). Reaches engine state via BuffHost; holds no monitor (runs inside the engine's held tick lock). Preserves the original null-spawner bare-return via `if (host.wavePhase == null) return`.
presentation/battle/engine/CombatResolver.kt       # #231/#230 (ADR-0012 Phase 4). Combat resolution lifted verbatim from GameEngine: onProjectileHitEnemy (+#17 dealt-gated knockback/lifesteal + bounce-target via host.aliveEnemies()), onOrbHitEnemy (private) + onOrbHit (public wrapper for the spawnOrbs binding), applyDamageToZiggurat (defense + death-defy + second-wind via host.consumeSecondWind() + <25% screen-shake), applyThorn (R3-02), handleWaveComplete + handleEnemyDeath (reward credit via host.simulation.creditCash using SimulationMath.killCashReward/waveCompleteCash, #146 derived-count comment, SimulationEvent boss/step emits, SCATTER child split). Owns its own CalculateDamage/CalculateDefense. Reaches engine state via CombatHost; holds no monitor (runs inside the engine's held tick lock / collision callbacks).
presentation/battle/engine/Entity.kt               # Abstract base: x, y, width, height, isAlive, update(), render(). V1X-09 Phase 3: implements pure-domain EntityProtocol — isAlive + isChronoSlowable (default false) + update + the x/y/width positional reads (now `override var`; the collision sweep reads them) — so Simulation.tickEntities + the collision sweeps can iterate without Canvas; height stays a plain var (not in the protocol); render() stays abstract.
presentation/battle/engine/WaveSpawner.kt          # Wave lifecycle: 26s spawn + 9s cooldown, enemy composition by wave; RO-11 #B.1 added optional `startWave: Int = 1` constructor param backing `var currentWave: Int = startWave` so BattleViewModel can map a WAVE_SKIP lab-research level to a higher initial wave (L0 → wave 1, L10 → wave 11). Enemy scaling at the higher wave is automatic via EnemyScaler. R3-02 changed `onMeleeHit: (Double) -> Unit` to `(EnemyEntity, Double) -> Unit` so consumers can react to the attacker reference (THORN_DAMAGE reflection); the field is forwarded as-is to each spawned `EnemyEntity.onMeleeHit`. V1X-15b added 2 pure helpers for the ENEMY_INTEL overlays: `getWaveComposition(wave): Map<EnemyType,Int>` (deterministic expected per-type counts mirroring the `pickType` probability bands, boss split off index 0 on boss waves) + `wavesUntilNextBoss(): Int`; backed by private `typeProbabilities(wave)`. #146: removed the hand-kept `enemiesAlive` tally (field + `++` in `spawnEnemy` + `onEnemyKilled()`) — it drifted negative (SCATTER children bypassed the only `++`; `onDeath` re-fires double-counted); the HUD count is now derived live by `GameEngine.aliveEnemyCount()`.
presentation/battle/engine/EnemyScaler.kt          # Wave-based stat scaling (1.05^wave × tierMultiplier), cash rewards per type
presentation/battle/engine/CollisionSystem.kt      # Projectile↔enemy and enemy projectile↔ziggurat collision. V1X-09 Phase 3 (ADR-0012, slice 4): reduced to a presentation adapter — keeps only the concrete-type concerns (filterIsInstance partitioning + alive snapshot) and delegates both sweeps to the pure-domain Simulation (detectProjectileEnemyHits / detectZigguratHits). Takes a Simulation param (GameEngine.update passes its `simulation`); the iteration + overlap geometry now live in the domain. #26 A28: `checkCollisions` now accepts pre-filtered typed lists (List<ProjectileEntity>, List<EnemyEntity>, List<EnemyProjectileEntity>) instead of partitioning the entity list itself — the partition moved to GameEngine under `entitiesLock` so the typed snapshots are already held when collision runs.
presentation/battle/entities/ZigguratEntity.kt     # 5-layer ziggurat, nearest-enemy targeting, HP tracking. V1X-09 Phase 2 (ADR-0012): HP/regen + attack-cooldown + rapidFireMultiplier + derived attackInterval/attackRange delegated to pure-domain ZigguratState; the entity's full public surface (currentHp/maxHp/stats/rapidFireMultiplier/attackRange/updateStats) delegates to the state so GameEngine/BattleViewModel are untouched. Keeps layer geometry + originX/Y/centerY + targeting/fire callback + render(). Constructor unchanged. RO-08 / R4-01: stats updates propagate live via updateStats (called by GameEngine.applyStats); attackInterval / attackRange read the live stats so in-round upgrades + GOLDEN_ZIGGURAT damage buff propagate mid-round. R4-01 dropped overdriveColor / overdriveProgress fields + the timer-bar render block + the timerBgPaint / timerFillPaint paints (used only by the timer bar).
presentation/battle/entities/ProjectileEntity.kt   # Moves toward target, self-destructs on arrival. V1X-09 Phase 2: motion delegated to pure-domain ProjectileState; this class keeps render() + the collision/bounce fields (damage/bouncesRemaining/hitEnemies). Constructor signature unchanged. #243: +`var trailTimer` (loop-thread-only, under entitiesLock; fresh per shot, never pooled) accumulating sim-time toward the next trail emission (see advanceTrail).
presentation/battle/entities/EnemyEntity.kt        # 6 enemy types, melee/ranged attack, mini HP bar. V1X-09 Phase 3: overrides EntityProtocol.isChronoSlowable = true (sole chrono-slowed entity; read by Simulation.tickEntities). V1X-09 Phase 2 (ADR-0012): movement + attack-cooldown delegated to pure-domain EnemyState; this class syncs x/y back to the Entity base each tick + keeps HP/armor/death, the melee/ranged attack callbacks, and render(). Constructor + spawner wiring unchanged. R3-02 changed `onMeleeHit: ((Double) -> Unit)?` to `((EnemyEntity, Double) -> Unit)?`; `update()` invokes `onMeleeHit?.invoke(this, damage)` so consumers see the attacker reference (THORN_DAMAGE reflection). Pre-R3-02 the chain dropped the attacker so `GameEngine.applyThorn` always early-returned despite being plumbed. (V1X-15b ENEMY_INTEL L5+ HP-% labels are drawn by `GameEngine.render` looping live `EnemyEntity`s, NOT in this class — keeps the level gate out of the entity constructor.) #146: `takeDamage` opens with `if (!isAlive) return 0.0` so a second projectile landing on a corpse in the same collision sweep can't re-fire `onDeath` (which would double-decrement the enemy count + re-credit the kill reward) — defense-in-depth complementing the #125 getAliveEnemies live re-derive.
presentation/battle/entities/EnemyProjectileEntity.kt # Ranged enemy projectiles targeting ziggurat; shooter field for thorn damage reflection. V1X-09 Phase 2: homing motion reuses the same pure-domain ProjectileState as ProjectileEntity (identical math); keeps render() + damage/shooter fields. Constructor unchanged.
presentation/battle/entities/OrbEntity.kt          # Orbiting projectiles circling ziggurat, per-enemy hit cooldown. #54 fix: orbit radius oscillates between ORBIT_RADIUS_MIN (25 px) and ORBIT_RADIUS_MAX (70 px) over ORBIT_PERIOD_SEC (2.5 s) so the inner sweep overlaps the enemy melee zone and the outer sweep places the orb cleanly outside HIT_RANGE. V1X-09 Phase 2: orbit position + radial-oscillation math delegated to pure-domain OrbState; OrbEntity keeps hit-cooldown/proximity (needs EnemyEntity refs + onHitEnemy callback) + render(), and re-exposes ORBIT_RADIUS_MIN/MAX + ORBIT_PERIOD_SEC + currentOrbitRadius via @VisibleForTesting from OrbState so the Robolectric OrbEntityTest is untouched. Constructor unchanged (initialRadialPhase still controls phase for tests).
presentation/battle/effects/ParticlePool.kt        # Pre-allocated particle pool (200 capacity), acquire/release/recycle
presentation/battle/effects/EffectEngine.kt        # Manages active effects, owns ParticlePool + ScreenShake. #191 CONC-1: effects/pendingEffects guarded by a private effectsLock — addEffect (off-thread), update (drain+snapshot under lock, per-effect update() outside, deferred removeAll in a 2nd lock acquisition → update→removeAll order preserved), render (snapshot under lock, draw outside), clear. pool/screenShake stay loop-confined (unguarded). Lock order entitiesLock→effectsLock (acyclic).
presentation/battle/effects/ScreenShake.kt         # Canvas translate oscillation with decaying amplitude
presentation/battle/effects/ReducedMotionCheck.kt  # System ANIMATOR_DURATION_SCALE reader
presentation/battle/effects/ProjectileTrailEffect.kt # Fading biome-colored trail particles behind projectiles. #243: +pure `advanceTrail(timer, dt)` helper + `TRAIL_INTERVAL = 0.03f` const — time-throttle so trail emission tracks elapsed projectile sim-time (≤ ~10 simultaneous particles/projectile at any speed), immune to the 2×/4× catch-up that thrashed the 200-slot pool. JVM-tested by ProjectileTrailThrottleTest; consumed by GameEngine's trail loop.
presentation/battle/effects/DeathEffect.kt         # Per-enemy-type death burst (6 types, 6-20 particles)
presentation/battle/effects/FloatingText.kt        # "+X Cash" rising text that fades
presentation/battle/effects/UWVisualEffect.kt      # 6 particle-based UW activation spectacles
presentation/battle/effects/WaveAnnouncement.kt    # Wave number slide-in + boss warning + cooldown countdown. V1X-15b: `WaveCooldownText` gained an optional `nextWaveComposition: String?` 2nd param (ENEMY_INTEL L1+ next-wave composition line, drawn below the timer).
presentation/audio/SoundManager.kt                 # SoundPool wrapper, 7 effects, volume/mute. V1X-05: `play(effect, expectedIntervalMs = 100L)` — frequency-aware SHOOT throttle `(interval/3).coerceIn(30,100)ms` so RAPID_FIRE + high attack-speed feel responsive without exhausting SoundPool channels.
presentation/audio/MusicManager.kt                 # V1X-06: MediaPlayer wrapper for 2 looping tracks (bgm_walking / bgm_battle). Audio focus listener pauses on phone call / Spotify; volume + mute control. MainActivity wires lifecycle (resume/pause/release) + nav-state observer that switches tracks when entering/leaving Battle. #246: createPlayer is nullable via an injectable `playerFactory` (default MediaPlayer::create, which returns null on codec/decode/OOM); a null degrades to silent so a later nav re-attempts. #242/ADR-0033: each track's player is built at most once OFF the main thread (injectable decodeExecutor; the 1.3MB OGG decode no longer hitches battle-nav), cached, switched via pause/seekTo(0)/start instead of release+recreate. Concurrency invariant: executor runs ONLY the decode; ALL state + MediaPlayer control on the main thread (decode result posted back via a main Handler). desiredTrack (last-write-wins) split from activeTrack; per-track pending flag dedups in-flight decodes (build-once-or-in-flight); release()-vs-in-flight + muted-deferred handled. Guarded by MusicManagerNullPlayerTest.
presentation/audio/MusicPreferences.kt             # V1X-06: SharedPreferences wrapper for music mute toggle + volume slider state. Stored under "music_prefs"; cleared by DataDeletionManager.
presentation/battle/ui/BattleControlRail.kt        # #171: left vertical control rail (speed 1x/2x/4x + pause + upgrade-toggle) at CenterStart, replacing the old bottom-center Row that overlapped the UW bar + upgrade menu. Pure presentational (state + callbacks, no VM ref); button bodies extracted verbatim from BattleScreen (only pause has haptics). Also holds `BattleControlRailDefaults.WIDTH` (the rail's fixed footprint). Modifier order width→verticalScroll→background→padding (pill wraps viewport). The upgrade menu clears this rail VERTICALLY (full-width sheet, `IN_ROUND_MENU_HEIGHT` short enough that its top sits below the rail's bottom) — no horizontal-padding coupling. Layout verified on-device (no Compose-rule coverage, PR-4736).
presentation/battle/ui/HealthBarRenderer.kt        # HP bar: green→yellow→red gradient, numeric text
presentation/battle/ui/InRoundUpgradeMenu.kt      # In-round upgrade menu: 3 tabs, purchase with Cash; RO-11 #C / RO-10 added optional `describeEffect: (UpgradeType) -> UpgradeEffectReadout` parameter (default no-op fallback) that drives a per-row Gold-#D4A843 "Now → Next" readout below the description. Suppresses the readout line when describeEffect returns empty current; renders "Now: X (MAX)" without arrow when next is null. V1X-13: cash/FREE/level-desc/readout/MAX/cost via stringResource (inround_*, cash_amount, upgrade_max); ✕ glyph + dynamic tab/type names left literal. #171: `fillMaxWidth()` sheet spans the full screen width along the bottom; fixed `IN_ROUND_MENU_HEIGHT` (top-level val, 240dp, was 280dp) is short enough that the bottom-anchored sheet's top sits below the left rail's bottom — clears the rail vertically so the rail stays tappable (list scrolls, no content lost).
presentation/battle/ui/PostRoundOverlay.kt         # Post-round summary: wave, kills, cash, time, new record banner. V1X-13: all banners/stat rows/ad buttons via stringResource + share text & chooser via getString in onClick (postround_*, cash_amount, steps_earned_banner). Bundle C (#162): the present highlight lines (record/tier/power-stones/steps) reveal via a staggered LaunchedEffect timer (delay 180ms/line, success() haptic each; instant + one haptic under reduced-motion); Play-Again button fires tap(). Entrance animation is hosted in BattleScreen.
presentation/battle/ui/PauseOverlay.kt             # Pause overlay: Resume + Quit Round buttons. V1X-13: title + buttons via stringResource (pause_title/action_resume/pause_quit_round).
presentation/battle/ui/BattleErrorOverlay.kt       # #190 REL-2: non-dismissable scrim shown when the game loop crashed (mirrors PauseOverlay). Single "Return to menu" action → onExitBattle. Caller suppresses round chrome via showGameChrome so nothing interactive sits behind it. Strings battle_error_title/body/return.
presentation/battle/ui/BiomeTransitionOverlay.kt   # Full-screen biome reveal overlay with step count. V1X-13: welcome/steps-walked/continue via stringResource (biome_*, action_continue); biome name dynamic.
presentation/battle/ui/UltimateWeaponBar.kt        # UW passive cooldown display (R4-06: no clickable activation buttons; auto-trigger replaces manual)
presentation/battle/biome/BiomeTheme.kt            # 5 biome color palettes (sky, ground, ziggurat, enemy, particles)
presentation/battle/biome/BackgroundRenderer.kt    # Gradient sky + ambient particle system per biome
presentation/ui/EnumDisplayName.kt                 # String.toDisplayName() — UPPER_SNAKE → "Title Case". #225: now the SINGLE UI enum-name formatter; the six `enum.name.replace('_',' ')` sites (TierSelector ×2, BattleViewModel, InRoundUpgradeMenu, UpgradeCard, UltimateWeaponScreen) were migrated to it; NoRawEnumNameInUiTest guards against regression
presentation/ui/CurrencyDisplay.kt                 # CurrencyType enum (STEPS/CASH/GEMS/POWER_STONES) + icon()/tint()/label() + CurrencyValue/CurrencyCost composables + formatCurrency(Long) (delegates to formatCount, #262 L87). Single source of truth for currency presentation — swap icon() to adopt themed-glyph art (#160). Material icons approximate for now (Diamond/DirectionsWalk/OfflineBolt/Paid)
presentation/ui/NumberFormatting.kt                # #262 L87: formatCount(Long) — single integer-grouping helper pinned to Locale.US (deterministic `,` grouping). Compose-free so service/StepWidgetProvider (RemoteViews) can share it; the prior 3 mechanisms (default-locale getNumberInstance(), "%,d".format, Locale.US formatCurrency) all route through it
presentation/ui/LoadingBox.kt                      # Full-screen centered CircularProgressIndicator; screen-level first-load gate across the menu screens (#160)
presentation/ui/EmptyState.kt                      # Centered empty-state (optional title + supporting message); shared by Cards + Workshop (#160)
presentation/ui/ErrorState.kt                      # #194/ADR-0028: full-screen terminal error state (icon + message + Retry) + SCREEN_LOAD_ERROR (i18n #34 phase 3: now a `@StringRes Int` = R.string.screen_load_error, not a String const; UiState.error is `Int?`, resolved via stringResource at the screen early-return; the .catch-inside-flatMapLatest order is unchanged); the 10 data-backed screens early-return it before the loading check when their VM data flow throws (Battle excluded — owns battleError). Each VM gained _retry/flatMapLatest/.catch (catch INSIDE flatMapLatest so retry() re-subscribes) + retry(). Sibling to LoadingBox/EmptyState
presentation/ui/UiMessage.kt                       # i18n #34 phase 3 (A): sealed `UiMessage` (@StringRes resId + optional args) for VM transient snackbar messages, resolved at the Compose call site via `fun UiMessage.resolve(Context)`. 6 VMs (Store/Cards/Workshop/Labs/Missions/Battle) emit `UiMessage?` (was `String?`), keeping them Context-free + pure-JVM testable (assert the type). `UiMessage.Raw(text)` = escape hatch for an ALREADY-localized lower-layer string (billing/ad Error.message); never wrap un-localized text in Raw. Guarded by UiMessageTest (Robolectric).
presentation/ui/SobTopAppBar.kt                    # Shared back-affordance app bar (CenterAlignedTopAppBar: centered title + ArrowBack, cd_back resource desc). Rendered once in MainActivity's outer Scaffold topBar, gated by Screen.secondaryTitle → appears on the 8 push-nav secondary screens only. Default TopAppBarDefaults.windowInsets (bar self-pads the status bar). Bundle B PR-B1 (#161); strings externalized i18n #34
presentation/ui/Haptics.kt                         # Haptics(view, prefs) helper: tap() (VIRTUAL_KEY) / success() (CONFIRM) over View.performHapticFeedback, each gated on HapticsPreferences.isEnabled() read at CALL time (Settings toggle takes effect next tap, no recompose). rememberHaptics() composable builds it from LocalView+LocalContext. No VIBRATE permission. Bundle C (#162)
presentation/ui/PurchasePulse.kt                   # Shared "a spend just happened" scale pulse: PulseState + rememberPulse() + Modifier.pulseScale(). animateFloatAsState 1f→1.12× tween(100) (snap under reduced-motion), graphicsLayer scale (no reflow), auto-resets after a 100ms LaunchedEffect hold. Extracted from the old inline UpgradeCard 1.05× pulse + enlarged; applied across all spend buttons. Bundle C (#162)
presentation/ui/ClaimCelebration.kt                # Structured ClaimReward (Bundle/Message/Generic) + ClaimCelebrationEvent(reward) + formatClaimReward/formatRewardParts (Compose-boundary formatter: plural + localized, "+5 Gems +2 Power Stones claimed!"; #260). ClaimCelebration(event, onConsumed) one-shot reward chip (scaleIn+fadeIn, fires success() haptic once, ~1.4s dwell; instant under reduced-motion). Hosted in Missions/Supplies screens; fed by the conflated VM celebration event. Bundle C (#162)
presentation/ui/EnumLabels.kt                      # #260: @StringRes label extensions for enums that surfaced raw CONSTANT_CASE — UpgradeCategory/PackTier/CardRarity/CosmeticCategory.labelRes() + wavePhaseLabelRes(String):Int? (BattleUiState.wavePhase is a String). i18n #34 phase 3 (G): added the domain-model display-copy resolvers here — descriptionRes() (Upgrade/Research/DailyMission/UltimateWeapon), nameRes() (Upgrade/Research/UltimateWeapon/Card/Biome/BattleCondition), Milestone.displayNameRes(), cosmeticNameRes/cosmeticDescRes(id), @Composable CardType.effectDescription(level), and (for Workshop) the stat-unit resources consumed by UpgradeCard. Byte-identical to the old `.toDisplayName()`/`description`/`effectDescriptionAtLevel` output (incl. quirks: UW_COOLDOWN→"Uw Cooldown"). Resolved via stringResource at the call site; domain enums stay Android-free (DomainPurityTest). Guarded by EnumLabelResTest + CardEffectDescriptionTest.
presentation/ui/Rarity.kt                          # Shared collectibles rarity identity (#163, Bundle D): RarityTier (3-tier palette) + plain color() (sand/LapisLight/Gold) + cardRarityTier(CardRarity)/uwRarityTier(unlockCost: <=60/61-89/>=90) + cardRarityLabel/uwRarityLabel (Cards COMMON/RARE/EPIC; UWs RARE/EPIC/LEGENDARY) + @Composable RarityBadge/EquippedChip (+alpha dim for locked UWs) + Modifier.rarityBorder (3dp border + left accent bar, clipped to card shape). Pure fns JVM-tested (RarityTest); composables visual-only. Used by CardsScreen + UltimateWeaponScreen.
presentation/ui/ColorLerp.kt                       # Pure colour-math for the onboarding biome-gradient cross-fade (#164, Bundle E): lerpArgb(a,b,t: packed-ARGB Int) per-channel interpolation (t clamped [0,1]) + crossfadeNeighborIndex(page,offset,lastIndex) signed-offset neighbour selection (clamped to [0,lastIndex] for overscroll edges). JVM-tested (ColorLerpTest, 10 cases).
presentation/ui/PrivacyPolicy.kt                   # #240: const PRIVACY_POLICY_URL — single source of truth for the hosted privacy-policy URL (same URL the Play Data-Safety form + site/index.md use). MainActivity.openPrivacyPolicy opens it via a guarded ACTION_VIEW intent from the Settings "Privacy Policy" row; PrivacyPolicyUrlTest pins the value (drift guard).
presentation/ui/theme/Color.kt                     # Compose color tokens: 5 brand colours (Gold/LapisLazuli/SandStone/DeepBronze/Ivory) + derived role tokens (LapisLight for lapis-on-dark text, BronzeSurface, TextPrimary/Secondary, StatusSuccess/Warning/Danger, GemColor/PowerStoneColor/StepColor) + RaritySand (collectibles rarity tier-0, #163; tier-1/2 reuse LapisLight/Gold). #213: +OnGold (#4A2618) text-role token for content on Gold buttons (~5.99:1; DeepBronze was ~4.19:1, AA-normal fail) + plain-Int GoldArgb/OnGoldArgb consts that ContrastTest reads to pin the real tokens ≥4.5:1.
presentation/ui/theme/Type.kt                      # SobTypography — deliberate Material3 type scale (weights/tracking/lineHeight). Display+Headline tiers + displayMedium/Large carry the custom Cinzel display face (#164, Bundle E, res/font/, OFL); Title/Body/Label stay Roboto. Wired via StepsOfBabylonTheme. Guarded by SobTypographyTest.
presentation/ui/theme/Shape.kt                     # SobShapes — small(8)/medium(14)/large(20) corner radii, wired via StepsOfBabylonTheme
presentation/ui/theme/Theme.kt                     # Compose theme setup (Material3): DarkColorScheme + SobTypography + SobShapes. onPrimary=OnGold (#213: dark-on-gold text ~5.99:1, was DeepBronze ~4.19:1), surfaceVariant=BronzeSurface
presentation/weapons/UltimateWeaponViewModel.kt    # @HiltViewModel: UW unlock/upgrade/equip state (R4-06: UWPathDisplay + UWDisplayInfo.paths for per-path UI)
presentation/weapons/UltimateWeaponScreen.kt       # UW management: 6 cards with lock/unlock/equip + 3 per-path Upgrade buttons (R4-06)
presentation/labs/LabsViewModel.kt                  # @HiltViewModel: research state + wallet + countdown ticker. R3-03: replaced the private `updateResearchMission()` helper with `private val updateMissionProgress = UpdateCompleteResearchMissionProgress(dailyMissionDao)`; all 3 call sites (init / rushResearch / freeRush) pass an explicit count (init: `completed.size` from CheckResearchCompletion; rush: 1 on Result.Rushed; freeRush: 1 after manual completeResearch). The DailyMissionType import was removed. #44: the researchList builder now calls `ResearchType.surfacedInLabs()` instead of an inline `entries.filterNot { it.isComingSoon }`; the `startResearch` `if (type.isComingSoon)` guard is kept as the reachable second layer (comment corrected: coming-soon types are filtered out of the list, not Start-button-suppressed). #211 (ADR-0036): read-only time-axis consumer — `init` evaluates `TimeIntegrity.evaluate(...)` and passes `verdict.newBaseline.trustedWallClock` to `checkCompletion(now = ...)`; never persists the baseline (DailyStepManager owns it).
presentation/labs/LabsUiState.kt                    # UI state: research list, slots, balances
presentation/labs/LabsScreen.kt                     # Labs screen: research cards, start/rush/unlock slot. #44: deleted the two dead `info.type.isComingSoon ->` branches (the "COMING SOON" title chip + the empty action arm) — unreachable since the VM filters coming-soon types out of the list via surfacedInLabs() before any card is built; no isComingSoon reference remains here.
presentation/cards/CardsViewModel.kt                # @HiltViewModel: card collection + wallet + pack/upgrade/loadout. #234: takes SavedStateHandle; the pack-reveal payload is stored as the PackRevealState DTO under key "packReveal" (written in openPack/watchFreePackAd, cleared in dismissPackResult) and mapped back to List<CardResult>? INSIDE the combine via toCardResultsOrNull() — survives process death; CardsUiState/screen unchanged.
presentation/cards/PackRevealState.kt               # #234: presentation-layer @Parcelize DTO (PackRevealState + RevealedCard) mirroring the transient pack-reveal payload for SavedStateHandle survival. CardType encoded by .name (decoded via valueOf; defensive toCardResultsOrNull() drops gracefully on a cross-version rename). Mappers toPackRevealState()/toCardResults()/toCardResultsOrNull() are top-level extensions in this file. Keeps domain CardResult/CardType pure (DomainPurityTest).
presentation/cards/CardsUiState.kt                  # UI state: owned cards, pack options, copy counts (copyCount / copiesNeeded — copy-based progression, R4-08/ADR-0010; Card Dust removed)
presentation/cards/CardsScreen.kt                   # Cards screen: pack opening, collection, equip/upgrade
presentation/supplies/UnclaimedSuppliesViewModel.kt  # @HiltViewModel: claim/claimAll supply drops. Bundle C (#162): conflated `celebration` Channel(receiveAsFlow) emits a ClaimCelebrationEvent — claimDrop emits SupplyDrop.toClaimReward() on Success; claimAll ONE aggregate ClaimReward.Message(reward_all_supplies) only if ≥1 Success (fold claims every drop, claim on left of `||` so no short-circuit skip). #260: structured ClaimReward, no concatenation.
presentation/supplies/SuppliesUiState.kt             # UI state: unclaimed drops list
presentation/supplies/UnclaimedSuppliesScreen.kt     # Inbox: drop cards, claim buttons, empty state
presentation/economy/CurrencyDashboardViewModel.kt   # @HiltViewModel: weekly challenge, login streak, balances
presentation/economy/EconomyUiState.kt               # UI state: weekly progress, streak, balances
presentation/economy/CurrencyDashboardScreen.kt      # Dashboard: weekly progress bar, streak dots, PS/Gem balances
presentation/missions/MissionsViewModel.kt           # @HiltViewModel: milestones + daily missions + claim flow. Bundle C (#162): conflated `celebration` Channel(receiveAsFlow) emits a structured ClaimCelebrationEvent(ClaimReward.Bundle) on claimMission/claimMilestone Success only (#260: gems/powerStones/cosmeticNames off the reward model, no concatenation; failure arms incl. the 3 UnknownCosmetic milestones never celebrate); @VisibleForTesting cancelForTest() stops the init while(true) ticker so JVM tests terminate.
presentation/missions/missionRewardLabel             # (top-level fn in MissionsViewModel.kt) pure celebration-label builder for a claimed mission — null/empty→"Reward claimed!", else "+G Gems +P Power Stones claimed!". Testable without the VM.
presentation/missions/MissionsUiState.kt             # UI state: missions list, milestones list, midnight countdown
presentation/missions/MissionsScreen.kt              # Missions screen: daily missions + walking milestones + claim
presentation/stats/StatsViewModel.kt                 # @HiltViewModel: walking history, battle stats, all-time aggregates. #234: takes SavedStateHandle; selected period is savedStateHandle.getStateFlow("selectedPeriod", WEEK) as a combine source (selectPeriod writes it back) → survives process death.
presentation/stats/StatsUiState.kt                   # UI state: bars, periods, battle/all-time stats
presentation/stats/StatsScreen.kt                    # Stats screen: chart, today, battle, all-time sections
presentation/stats/WalkingHistoryChart.kt            # Canvas-drawn bar chart with period toggle
presentation/settings/SettingsViewModel.kt             # @HiltViewModel: notification preference toggles + sound/music prefs + haptics pref (Bundle C #162: hapticsPrefs param + hapticsEnabled state + setHapticsEnabled, mirrors the sound/music setters) + data deletion
presentation/settings/SettingsScreen.kt                # Settings screen: 4 notification toggles, sound/music controls, "Haptic Feedback" toggle (Bundle C #162, in the Sound group), "Replay tutorial" row (onReplayTutorial callback → re-enters onboarding), "Background activity" row (#261/ADR-0029, onOptimizeBattery callback → MainActivity fires ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS; durable re-offer since onboarding is one-shot), Delete All Data
presentation/onboarding/OnboardingSlide.kt             # Pure-Kotlin slide model + canonical 4-slide OnboardingContent list (final slide is the permission primer). +biome: Biome? (per-slide BiomeTheme gradient source) + art: OnboardingArt? (slide-1 ziggurat emblem) + OnboardingArt marker enum — both fields pure, JVM-testable (#164, Bundle E)
presentation/onboarding/OnboardingViewModel.kt         # @HiltViewModel: exposes the slide list; completeOnboarding() persists the OnboardingPreferences flag; stepSensorAvailable (#193); shouldOfferBatteryExemption (#261/ADR-0029 = !batteryOptimizationStatus.isIgnoring(), read once at construction)
presentation/onboarding/OnboardingScreen.kt            # First-launch tutorial carousel (HorizontalPager): swipeable slides, Skip→primer, permission-primer final slide w/ granted/ask/denied states + Settings recovery. Per-slide biome sky-gradient behind the pager (cross-faded via lerpArgb/crossfadeNeighborIndex on pager offset; static under reduced-motion) + text scrim + slide-1 vector ziggurat emblem (else emoji) + one-shot completion pulse (reuses PurchasePulse; persist-first → pulse → navigate, gating contract preserved) (#164, Bundle E)
presentation/store/StoreViewModel.kt                   # @HiltViewModel: billing + cosmetic purchase actions; init calls billingManager.reconcilePendingPurchases() on Store entry (C.5 PR 2). #249: the 3 billing purchase fns delegate to a private runPurchase(product) helper that surfaces PurchaseResult.Error.message via _userMessage (Store Snackbar) — network/pending/cancel no longer swallowed; purchaseCosmetic (Gem spend) keeps its own failure path.
presentation/store/StoreUiState.kt                     # UI state: gems, adRemoved, seasonPass, cosmetics, priceDisplays (Map<BillingProduct, String> populated from BillingManager.getPriceDisplay; missing keys signal UI fallback to BillingProduct.priceDisplay constant; Plan 31 PR B)
presentation/store/StoreScreen.kt                      # Store screen: Gem packs, Ad Removal, Season Pass, Cosmetics
presentation/help/HelpScreen.kt                        # Help / how-to-play screen (backs the Help nav route; registered in MainActivity's NavHost)
```

## Resources

All paths relative to `app/src/main/res/`.

```
drawable/ic_launcher_background.xml     # Solid #0E2247 deep-lapis vector background for the adaptive launcher icon. Solid (not gradient) for consistent rendering across launcher mask shapes.
drawable/ic_launcher_foreground.xml     # 5-tier stepped-ziggurat silhouette vector, single compound path, vertical 3-stop linear gradient Gold #D4A843 (top) → SandStone #C2B280 (mid) → lightened-DeepBronze #8B5A3A (bottom). All content inside the 72dp safe zone; tower center (54,54) matches canvas center.
drawable/ic_ziggurat_emblem.xml         # Flat-vector stepped-ziggurat emblem in a gold roundel (4 trapezoid layers + ivory finial, 108×108 viewport). Onboarding slide-1 icon, replaces the 🏛️ temple emoji; rendered at 96dp, decorative (#164, Bundle E).
font/cinzel_regular.ttf, font/cinzel_bold.ttf  # Bundled Cinzel display font (SIL OFL 1.1) — weights 400 + 700, the app's custom display face. Referenced by the Cinzel FontFamily in ui/theme/Type.kt (#164, Bundle E). License text at repo licenses/OFL-Cinzel.txt (NOT in res/font/ — the resource merger rejects non-font files there).
mipmap-anydpi-v26/ic_launcher.xml       # <adaptive-icon> pointing at the drawable/ background + foreground pair.
mipmap-anydpi-v26/ic_launcher_round.xml # Same contents as ic_launcher.xml — Android handles round masking from the adaptive source; no separate round asset needed.
values/strings.xml                      # String resources (~114): app_name + notification strings (notif_*, notif_balance) + fx_* battle floating-text + battle/workshop Compose strings (V1X-13 i18n phase 1) + #260 enum-label/template strings (enemy_*, wave_composition*, upgrade_cat_*, pack_tier_*, rarity_*, uw_rarity_*, cosmetic_cat_*, wave_phase_*, reward_* claim-celebration templates, widget_balance, reminder_*).
values/plurals.xml                      # #259: 13 count-driven <plurals> (one/other) for grammatically-correct quantity nouns — fx_step_reward, steps_earned_banner, wave_enemies, boss_in_waves, reward_gems/power_stones/steps, card_copies, days_remaining, page_x_of_n, widget_steps, reminder_steps_away, notif_today_steps. Compose → pluralStringResource; off-Compose → getQuantityString. Guarded by PluralsResourceTest.
values/themes.xml                       # App theme Theme.StepsOfBabylon — NoActionBar parent + windowBackground=@color/brand_deep_bronze (kills the default ActionBar app-wide + cold-start white flash). Referenced by AndroidManifest android:theme.
values/colors.xml                       # XML-side colour resources for theme/window use only (Compose colours live in ui/theme/Color.kt). brand_deep_bronze (#6B3A2A) = window background.
xml/network_security_config.xml         # Network security config (cleartext blocked).
xml/step_widget_info.xml                # AppWidget provider info for the 2×2 step widget.
raw/                                    # Placeholder sine-wave sound assets.
layout/                                 # widget_step_counter.xml — 2×2 step widget layout (battle renderer is SurfaceView, not Compose/XML). V1X-13: title uses @string/app_name; the 2 runtime-set TextViews use design-time tools:text (no android:text) so the HardcodedText lint-as-error guard (app/build.gradle.kts `lint { error += "HardcodedText" }`) stays clean. HardcodedText is XML-only — does NOT catch Compose Text() (see docs/architecture.md i18n section).
```

## Tools & Release Assets

```
tools/render_play_store_icon.py                       # Pillow-only renderer that reproduces the in-app vector adaptive icon as a 512×512 PNG for Play Store hi-res upload. Re-renders pixel-for-pixel from the same 108-viewport path coords + 3-stop gradient stops as the XML drawables. Supersamples to 2048×2048 before LANCZOS downsample for crisp anti-aliased edges. Run via `python3 tools/render_play_store_icon.py`; if the icon design ever changes, edit BOTH this script AND the matching XML.
docs/release/store-assets/StepsOfBabylonArt.png                              # User-supplied source for the Play Store feature graphic. 1376×768 pixel-art Tower of Babel scene (ziggurat tower, swirling sky, walking figure, framing ruins). Preserved in-repo for re-crops.
docs/release/store-assets/play-store-icon-512.png                            # Play Store hi-res app icon, 512×512, ~3.8 KB. Generated artifact — regenerate via the script above when source changes. Tracked in git as a release asset (Play Store assets repository pattern).
docs/release/store-assets/play-store-feature-graphic-1024x500.png            # Play Store feature graphic, 1024×500, ~621 KB. Center-vertical-cropped (y=48..720) from StepsOfBabylonArt.png + LANCZOS-downscaled. PNG (not JPEG) preserves pixel-art crispness.
app/src/release/generated/baselineProfiles/baseline-prof.txt                 # Committed Baseline Profile (18,804 rules) for the cold-launch→Home path. Consumed by R8/profileinstaller at release build to pre-JIT-compile the critical startup path. Generated by the :baselineprofile module; committed so CI and Play distribution always have an up-to-date profile. Regenerate by running the BaselineProfileGenerator connected benchmark. (#26 Gate-G)
docs/performance/battery-audit.md                                             # Gate-G battery-impact audit: methodology, measurement results, pass/fail verdict. (#26)
docs/performance/startup-baseline.md                                          # Gate-G cold-start timing baseline: measurement methodology, pre/post profile numbers, target thresholds. (#26)
```

## Gradle / CI Modules

All paths are repo-root-relative. These modules are siblings of `:app` in `settings.gradle.kts`.

```
baselineprofile/build.gradle.kts                                              # :baselineprofile com.android.test module. Applies the androidx.baselineprofile plugin; targets :app with `targetProjectPath`; `BaselineProfileRule` + UiAutomator drive the cold-launch→Home path. Outputs the profile consumed at `app/src/release/generated/baselineProfiles/baseline-prof.txt`. (#26 Gate-G)
baselineprofile/src/main/AndroidManifest.xml                                  # AndroidManifest for the :baselineprofile test module.
baselineprofile/src/main/java/com/whitefang/stepsofbabylon/baselineprofile/BaselineProfileGenerator.kt  # BaselineProfileRule test that drives cold launch to the Home screen and captures startup HRF rules for the committed Baseline Profile. (#26 Gate-G)
macrobenchmark/build.gradle.kts                                               # :macrobenchmark com.android.test module. Applies the androidx.benchmark plugin; targets :app. Runs StartupTimingMetric (cold start) + FrameTimingMetric journey benchmarks. (#26 Gate-G)
macrobenchmark/src/main/AndroidManifest.xml                                   # AndroidManifest for the :macrobenchmark test module.
macrobenchmark/src/main/java/com/whitefang/stepsofbabylon/macrobenchmark/StartupBenchmark.kt  # Macrobenchmark: cold-start StartupTimingMetric (measures time-to-fully-drawn) against the release-signed :app. (#26 Gate-G)
macrobenchmark/src/main/java/com/whitefang/stepsofbabylon/macrobenchmark/JourneyBenchmark.kt  # Macrobenchmark: FrameTimingMetric journey (measures jank on a representative navigation path). (#26 Gate-G)
```

## Lint Config & Tooling

All paths repo-root-relative. Wired into the `build-and-test` CI job (code-gated). ADR-0037.

```
lint-kotlin.sh                    # Committed ktlint runner script (SHA-256-verified download of ktlint 1.8.0 in CI; CHECK mode with baseline, FORMAT mode without). `./lint-kotlin.sh` to check; `./lint-kotlin.sh --format` to auto-fix.
.editorconfig                     # Root EditorConfig — configures ktlint's formatting rules (indent, max line length, import ordering, etc.). Consumed by ktlint and most IDEs.
config/detekt/detekt.yml          # detekt custom config (2.0.0-alpha.5): disables MagicNumber + WildcardImport; no `formatting:` ruleset (doesn't exist in detekt 2.0 without a plugin).
config/detekt/baseline.xml        # detekt baseline (502 lines): grandfathers existing violations; only NEW violations fail `:app:detekt`.
config/ktlint/baseline.xml        # ktlint baseline (10141 lines): grandfathers existing formatting violations; only NEW violations fail the check.
```

## Service Layer

```
service/StepCounterService.kt        # Foreground service (health type), START_STICKY, collects sensor flow. #244: onCreate routes startForeground() through the top-level `startForegroundSafely(start,onFailure)` seam — catches RuntimeException (FGS-not-allowed/invalid-type/SecurityException) → Log.w + stopSelf() so a typed-FGS start failure can't kill the process (WorkManager catches up). Guarded by StartForegroundSafelyTest.
service/StepNotificationManager.kt   # Notification channel + builder, 30s throttled updates
service/SupplyDropNotificationManager.kt # Supply drop notification channel + deep-link to inbox. #216: gates the push on NotificationPolicy (quiet-hours + 3/day cap) via an injected TimeProvider; per-day counter in a field-cached "supply_drop_notifications" SharedPreferences (field-cached because notify() runs under DailyStepManager's #120 credit mutex — no disk load under the lock). The drop is still generated + claimable; only the push is suppressed. Guarded by SupplyDropNotificationManagerTest (Robolectric).
service/BootReceiver.kt              # BOOT_COMPLETED → restart StepCounterService (#244: startForegroundService wrapped in try/catch(RuntimeException) — a background BOOT start can be FGS-not-allowed; don't crash the receiver).
service/StepSyncWorker.kt            # @HiltWorker CoroutineWorker, 15-min periodic: sensor catch-up + HC sync. #123: catch-up arithmetic extracted to a pure companion `computeCatchUp(dayStart,currentCounter,alreadyCredited,sensorStepsAtDayStart)` → CatchUpDecision (Establish/Rebaseline/Credit/Skip); detects a mid-day reboot (currentCounter < dayStart) and re-anchors the baseline + offset so post-reboot steps credit instead of being dropped. Guarded by StepIngestionTest (now delegating to the real computeCatchUp). #250/ADR-0028: also calls the top-level `reconcileBillingSafely(billingManager)` helper (withTimeoutOrNull(20s) + catch-all) as a background safety net for pending/offline purchases; helper is JVM-unit-testable (ReconcileBillingSafelyTest) — also called by MainActivity.onResume
service/StepSyncScheduler.kt         # Enqueues periodic WorkManager request
service/SmartReminderManager.kt      # Upgrade proximity reminders (piggybacked on StepSyncWorker). #216: a quiet-hours early-return via NotificationPolicy.canSendReminder(LocalTime.now()), before the existing last_sent 1/day write (not under the #120 mutex).
service/MilestoneNotificationManager.kt # Wave record and step milestone notifications
service/StepWidgetProvider.kt        # 2×2 AppWidgetProvider for home screen widget
service/WidgetUpdateHelper.kt        # Throttled widget update helper
```

## Test Layer

All paths relative to `app/src/test/java/com/whitefang/stepsofbabylon/` unless noted otherwise. The `androidTest` source set lives at `app/src/androidTest/java/com/whitefang/stepsofbabylon/` and is documented at the bottom of this section (V1X-08 Phase 1A).

```
architecture/DomainPurityTest.kt                 # Machine-enforced domain-purity guard (#27/#228): dependency-free JVM test. Test 1 — fails if any domain/ file imports android./androidx./com.android./com.google.android./com.whitefang.stepsofbabylon.data (the full dependency rule, #228 added the data prefix). Test 2 — fails if domain/ imports dagger./javax.inject. (locks DI-agnosticism). Self-validating (asserts domain root dir exists first). KDoc-link `[…data…]` refs are not import lines, so ignored. 2 tests.
domain/notification/NotificationPolicyTest.kt    # #216: pure-JVM tests for NotificationPolicy — quiet-hours boundaries (21:59 allowed / 22:00 / 00:00 / 07:59 quiet / 08:00 allowed / midday; midnight-crossing window), canSendReminder gate, and supply-drop cap (0..cap-1 allowed / cap / cap+1 blocked / quiet-hours blocks regardless of count). 14 tests.
architecture/PresentationPurityTest.kt           # (#219/#229) presentation→data guard: fails if any presentation/ file imports a data.local.*Dao / data.local.AppDatabase / data.local.*Entity. Allowlist = BattleViewModel.kt's AppDatabase (the end-of-round withTransaction atomicity seam, ADR-0035). Non-local data.* imports (data.time/onboarding/sensor/…) are out of scope. Same import-line-scan limitation as DomainPurityTest. 1 test.
architecture/StepCreditAllowlistTest.kt          # (#371, ADR-0038, ADR-0003) "Steps never generated in-game" guard: dependency-free JVM source-scan (mirrors DomainPurityTest — comment-stripped, qualified-call). Pins the FULL PlayerProfileDao currentStepBalance write surface: .addSteps( callers=={ClaimSupplyDrop,StepCrossValidator,DailyStepManager}; positive .adjustStepBalance( =={PlayerRepositoryImpl,DailyStepDao} (negated debit excluded); .updateStepBalance( has zero callers; PlayerProfileEntity( construction confined to PlayerRepositoryImpl; DAO `currentStepBalance =` write-count pin (==3); + a baked negative fixture proving each matcher goes red on a rogue site. 6 tests.
architecture/BattleEngineLockScanTest.kt         # (#372, ADR-0038) battle-engine "collaborators hold no monitor of their own" guard: dependency-free JVM source-scan. FAILS if {UWController,CombatResolver,BuffTickers,BattleRenderer} declares synchronized(/ReentrantLock/= Any()/= Object(). Comment-stripped; excludes GameEngine (sole entitiesLock owner) + BattleHosts (KDoc mention). 1 test.
fakes/FakePlayerRepository.kt                    # In-memory StateFlow-backed fake for PlayerRepository
fakes/FakeWorkshopRepository.kt                  # In-memory StateFlow-backed fake for WorkshopRepository
fakes/FakeUltimateWeaponRepository.kt            # In-memory StateFlow-backed fake for UltimateWeaponRepository (R4-06: rewritten for upgradePathLevel + per-path level tracking)
fakes/FakeLabRepository.kt                       # In-memory StateFlow-backed fake for LabRepository
fakes/FakeCardRepository.kt                      # In-memory StateFlow-backed fake for CardRepository
fakes/FakeWalkingEncounterRepository.kt          # In-memory StateFlow-backed fake for WalkingEncounterRepository
fakes/FakeStepRepository.kt                      # In-memory StateFlow-backed fake for StepRepository. #227: now also carries the battle/boss atomic-credit emulation that FakeDailyStepDao had — per-day battleStepsEarned/bossPsEarnedToday counters under a Mutex, linkedPlayer wallet routing, creditBattleStepsAtomicCallCount/creditBossPsAtomicCallCount, + seed/get test accessors (so the Award*Test cap/partial-credit/concurrency assertions survive the port migration)
fakes/FakeMissionRepository.kt                   # (#227) wraps FakeDailyMissionDao (exposes it as `.dao` for direct seeding); maps DailyMission↔entity null-skipping unknown missionType
fakes/FakeMilestoneRepository.kt                 # (#227) wraps FakeMilestoneDao (exposes `.dao` + claimMilestoneAtomicCallCount + upsert); supplies a mockito decoy PlayerProfileDao the wrapped DAO ignores (linkedPlayer routing)
fakes/FakeDailyLoginRepository.kt                # (#227) in-memory map fake for DailyLoginRepository
fakes/FakeWeeklyChallengeRepository.kt           # (#227) in-memory map fake for WeeklyChallengeRepository
fakes/FakeCosmeticRepository.kt                  # In-memory fake for CosmeticRepository
fakes/FakeBillingManager.kt                      # Configurable fake for BillingManager (+reconcileCallCount for C.5 PR 2 hook assertion + priceDisplayOverrides Map for Plan 31 PR B getPriceDisplay; private set on counter)
fakes/FakeRewardAdManager.kt                     # Configurable fake for RewardAdManager
fakes/FakeMilestoneDao.kt                        # In-memory fake for MilestoneDao
fakes/FakeDailyMissionDao.kt                     # In-memory fake for DailyMissionDao. #127: insert() models the real onConflict=IGNORE on (date,missionType) — a duplicate tuple is dropped rather than producing a second row.
fakes/FakeDailyLoginDao.kt                       # In-memory fake for DailyLoginDao
fakes/FakeWeeklyChallengeDao.kt                  # In-memory fake for WeeklyChallengeDao
fakes/FakeDailyStepDao.kt                        # In-memory fake for DailyStepDao with Flow support; #121: implements the 4 column-targeted upserts (copy-only-their-fields, mirroring the real ON CONFLICT DO UPDATE SET column semantics)
fakes/FakeCosmeticDao.kt                         # In-memory fake for CosmeticDao with auto-increment id simulation (C.2 PR 2)
fakes/FakeWorkshopDao.kt                         # In-memory fake for WorkshopDao (issue #19/#20 fix bundle) — supports repo-impl seeding tests; default `purchaseUpgradeAtomic` interface body inherited unchanged so future atomic-purchase tests can opt in by supplying a real PlayerProfileDao
fakes/FakeLabDao.kt                              # In-memory fake for LabDao (issue #19/#20 fix bundle) — supports repo-impl seeding tests on the per-`researchType` primary key
fakes/FakeTimeProvider.kt                        # Mutable TimeProvider with var fixedDate / fixedInstant (B.1)
fakes/FakeTimeBaselineSource.kt                  # #211: in-memory TimeBaselineSource for the VM time-axis tests (plain-JVM, can't construct a Context-backed AntiCheatPreferences) — settable baseline + reading.
domain/usecase/CalculateUpgradeCostTest.kt        # Cost formula: baseCost × scaling^level, all 24 types
domain/usecase/CanAffordUpgradeTest.kt            # Affordability checks against wallet
domain/usecase/QuickInvestTest.kt                 # Cheapest affordable upgrade recommendation
domain/usecase/PurchaseUpgradeTest.kt             # Purchase flow with fake repos
domain/usecase/UpdateBestWaveTest.kt              # Best wave tracking, new record detection
domain/usecase/CheckTierUnlockTest.kt             # Tier unlock logic against wave milestones
domain/usecase/UnlockUltimateWeaponTest.kt        # UW unlock with Power Stones
domain/usecase/UpgradeUltimateWeaponTest.kt       # UW upgrade cost scaling, max level
domain/usecase/ResolveStatsTest.kt                # Multiplicative stacking, all stat caps
domain/usecase/CalculateDamageTest.kt             # Crit/no-crit with injectable Random, damage/meter bonus
domain/usecase/CalculateDefenseTest.kt            # Percent reduction, flat block, floor at 0
domain/usecase/CalculateResearchCostTest.kt       # Research cost scaling: baseCost × 1.15^level
domain/usecase/CalculateResearchTimeTest.kt       # Research time scaling: baseTime × 1.10^level
domain/usecase/StartResearchTest.kt               # Start research: slots, affordability, max level, deducts Steps
domain/usecase/CompleteResearchTest.kt            # Complete research: timer gating, level increment
domain/usecase/RushResearchTest.kt                # Rush research: linear Gem cost (50–200), deducts Gems
domain/usecase/UnlockLabSlotTest.kt               # Unlock slot: 200 Gems, max 4
domain/usecase/CheckResearchCompletionTest.kt     # Auto-complete: expired research, skip not-ready. #211: + cases proving a trusted-now (capped) gate vs a raw forward-jumped now (auto-completion does not fire on the jump).
domain/time/TimeIntegrityTest.kt                  # #211 (ADR-0036): pure-JVM exhaustive cases for TimeIntegrity.evaluate — first-run seed, steady advance, backward rollback (reboot-durable maxWallClockSeen floor), in-session forward jump (excess capped out of trustedWallClock), reboot fallback (elapsedDelta<0 → full wallDelta), order-independence of the capped-accrual anchor. 7 tests.
domain/usecase/UpdateCompleteResearchMissionProgressTest.kt # 5 R3-03 tests covering count gating + multi-completion cap-at-target + no-row defensive path. Negative cases (count=0, count=-1) prove the gating; positive cases (count=1, count=5 capped at target=1) prove the additive-with-cap math; the missing-row case asserts idempotency.
domain/usecase/OpenCardPackTest.kt                # Pack opening: gem deduction, rarity rolling, duplicates increment copyCount (R4-08 copy-based, ADR-0010)
domain/usecase/UpgradeCardTest.kt                 # Card upgrade: copies-per-level cost by rarity (3/4/5), max level 7 (R4-08 copy-based, ADR-0010)
domain/usecase/ApplyCardEffectsTest.kt            # All 9 card effects, level scaling, buff+debuff combos
domain/usecase/ManageCardLoadoutTest.kt           # Equip/unequip, loadout full
domain/usecase/GenerateSupplyDropTest.kt          # Drop generation: inbox cap, milestone, threshold, random triggers
domain/usecase/ClaimSupplyDropTest.kt             # Claim flow: reward crediting, already-claimed guard
domain/usecase/AwardWaveMilestoneTest.kt          # Wave milestone PS: 1/2/5 at wave boundaries
domain/usecase/CheckMilestonesTest.kt             # Milestone detection: threshold, claimed exclusion
domain/usecase/ClaimMilestoneTest.kt              # Claim flow: reward crediting, idempotent guard, concurrent-claim atomicity (B.2 PR 4); post-C.2 PR 3b/3c all 3 milestone cosmetic claims (IRON_SOLES, MARATHON_WALKER, GLOBE_TROTTER) have end-to-end success tests via the real CosmeticRepositoryImpl + FakeCosmeticDao; synthetic rejection-before-atomic regression guard remains for mechanism coverage (C.4 + C.2 PR 3 + PR 3b + PR 3c)
domain/usecase/GenerateDailyMissionsTest.kt       # Mission generation: deterministic, one per category
domain/usecase/PurchaseGemPackTest.kt             # Gem pack purchase: delegates to billing, error forwarding
domain/usecase/CombatPowerTest.kt                 # (#29) Combat-power proxy formula: damage/attack-speed/crit-chance each raise it; crit factor only matters once crit chance > 0; monotonic; pure-JVM.
domain/usecase/WorkshopLevelsTest.kt              # (#29) levelOf default 0; isAtMax against config maxLevel (and always-false for uncapped); withIncremented bumps the workshop dimension by one without clamping; pure-JVM.
domain/usecase/EvaluateUpgradeValueTest.kt        # (#29) Δpower>0 candidate filter (non-combat + Crit-Factor-at-0-crit excluded, maxed/cost-0 excluded), value/percentPerKSteps math, barFraction normalisation, single-Best-Buy selection (affordable-first then highest-value-overall greyed fallback), ordinal tie-break; pure-JVM.
domain/usecase/DescribeUpgradeEffectTest.kt       # RO-11 #C / RO-10 readout coverage + RO-12 card / precision regressions + #29 workshopPreview: 39 tests total — 1 multiplicative test per stat-bearing upgrade (DAMAGE / ATTACK_SPEED / RANGE / HEALTH / HEALTH_REGEN \"2-decimal format\" / KNOCKBACK), 1 additive test per percentage-cap upgrade (CRITICAL_CHANCE / CRITICAL_FACTOR / DEFENSE_PERCENT / DEFENSE_ABSOLUTE / THORN_DAMAGE / LIFESTEAL clamp at 15 % / DAMAGE_PER_METER / DEATH_DEFY clamp at 50 %), 1 discrete test per threshold-crossing upgrade (MULTISHOT / BOUNCE_SHOT / ORBS), 1 utility test per cash-utility upgrade (CASH_BONUS / CASH_PER_WAVE / INTEREST cap 10 % / FREE_UPGRADES cap 25 % \u2014 last two also assert next == null at max level), 2 hidden-but-tested-for-Workshop-reuse (STEP_MULTIPLIER cap 100 % / RECOVERY_PACKAGES cap 50 %), 1 lab-research-stacks (DAMAGE_RESEARCH outer multiplier \u00d7 DAMAGE), 1 smoke test that every UpgradeType produces a non-empty current readout (catches future enum additions falling through to a missing case branch). RO-12 adds 3 net new tests: `RO12 HEALTH_REGEN Lv 0 to Lv 1 produces a visibly different readout` (precision regression direct guard, asserts "1.00/s" → "1.02/s"), `RO12 HEALTH readout reflects equipped WALKING_FORTRESS card multiplier` (Bug 3 regression: ws=5 HEALTH + WALKING_FORTRESS Lv 1 → "1725 HP", verifies card effects are post-applied to mirror the live engine pipeline), `RO12 HEALTH readout with no cards equipped is unchanged from pre-RO12 baseline` (default-arg behaviour preserved for the 25 existing tests). #29 adds 4 `workshopPreview` tests (workshop-dimension Now→Next, next==null at max, shared-WorkshopLevels parity with the value delta).
domain/model/MilestoneTest.kt                     # 6 milestones: thresholds, rewards, sorting
domain/model/DailyMissionTypeTest.kt              # 6 mission types: targets, rewards, categories
domain/model/TierConfigTest.kt                    # All 10 tiers, battle conditions, invalid tier
domain/model/BiomeTest.kt                         # All tier→biome mappings
domain/model/CardLoadoutTest.kt                   # Max 3, no duplicates, add/remove
domain/model/CardTypeTest.kt                      # 32 #53-regression tests covering effectDescriptionAtLevel: 3 named tests per card (Lv1 matches effectLv1 / Lv4 visibly different / Lv7 matches effectLv7) for all 9 cards + 4 cross-card invariants (entry count, no-blank smoke, Lv1≠Lv7 regression guard, Lv4 differs from both ends for the 8 continuous-progression cards). Lv1≠Lv7 invariant directly fails on `main` pre-fix where every level rendered effectLv1 verbatim.
domain/model/UltimateWeaponLoadoutTest.kt         # Max 3, no duplicates, add/remove
domain/model/UpgradeTypeTest.kt                   # 24 entries, category counts (9 attack / 9 defense / 6 utility), valid configs
domain/model/EnemyTypeTest.kt                     # 6 entries, multiplier correctness
domain/model/BattleConditionEffectsTest.kt        # All tier condition modifiers verified
domain/model/ResearchTypeTest.kt                  # RO-11 #B.2 set-equality contract: only AUTO_UPGRADE_AI is flagged `isComingSoon` (ENEMY_INTEL was flipped to wired in V1X-15b). Catches both directions of regression — a deferred enum silently flipping back to wired (re-introduces the dead-enum gap), or a wired enum silently getting hidden from the Labs UI. #44: +2 tests pinning `surfacedInLabs()` — `surfacedInLabs excludes coming-soon research` (none isComingSoon + no AUTO_UPGRADE_AI) and `surfacedInLabs is exactly the wired types` (set-equality: entries − AUTO_UPGRADE_AI). Guards the helper BODY (the call-site wiring is held by code review + the VM guard, per the spec's accepted coverage boundary).
presentation/battle/engine/EnemyScalerTest.kt     # Wave scaling, speed, cash rewards
presentation/battle/engine/WaveSpawnerTest.kt     # RO-11 #B.1 regression guard: `currentWave` reads from the constructor's `startWave` argument so BattleViewModel can map a WAVE_SKIP lab-research level to a higher initial wave (L10 → wave 11). The default `startWave = 1` path is implicitly covered by every existing GameEngineTest — a regression there would surface in unrelated tests. R3-02 updated `onMeleeHit` lambda type to the 2-arg `(EnemyEntity, Double) -> Unit` shape. v15-soak updated `onEnemyFireProjectile` to the 6-arg `(EnemyEntity, Float, Float, Float, Float, Double) -> Unit` shape for ranged thorn damage (#61). V1X-15b +3: `getWaveComposition` early non-boss deterministic, boss wave includes exactly one BOSS, `wavesUntilNextBoss` counts forward from currentWave.
presentation/battle/engine/GameEngineConcurrencyTest.kt # #118: GameEngine.entities thread-safety — structural mutation/iteration behind entitiesLock; concurrent loop-thread + UI-thread access doesn't corrupt the list
presentation/battle/engine/CollisionSystemScratchTest.kt # #26 A28: Robolectric parity tests verifying the pre-filtered typed-list collision sweep (scratch-buffer path) produces identical hit outcomes to the previous filterIsInstance path. Covers projectile↔enemy overlap / no-overlap / dead-enemy skip and ziggurat-hit / miss cases. JUnit 4 + Robolectric (Android Paint dependency from entity construction).
presentation/battle/engine/ChronoOverlayPaintTest.kt    # #26 A31: Robolectric identity test asserting the cached `chronoOverlayPaint` field is the same object instance across successive render calls (no per-frame Paint allocation). Uses `setChronoActiveForTest` seam to activate the overlay path without a full game loop. JUnit 4 + Robolectric.
presentation/battle/engine/GameEngineTest.kt      # RO-08 + RO-09 + R3-02 regression guards. RO-08 (post-R4-01, Step Overdrive removed): updateEffectiveLevels propagates CASH_BONUS to subsequent kill rewards; RECOVERY_PACKAGES heal pulse fires once per interval in SPAWNING phase, with level 0 / full HP / cap-50% all guarded. (The original ASSAULT/FORTRESS overdrive tests + `expireOverdrive` helper were dropped when R4-01 deleted Step Overdrive.) RO-09 #1: chrono active slows enemies to ~10% of baseline (per-entity dt scaling, regression-guarded with 0.08..0.12 tolerance band); chrono inactive deterministic across fresh engines; projectiles unaffected (200 px/sec movement preserved). RO-09 #2: 4 fortuneMultiplier-stacking guards covering all 3 production sites — GOLDEN expiry preserves FORTUNE 3.0×, GOLDEN expiry resets to 1.0× when ASSAULT active (regression for the 5.0×-leak-across-non-FORTUNE-overdrive bug), FORTUNE activation does not downgrade GOLDEN's 5.0× via `coerceAtLeast(3.0)`, expireOverdrive preserves GOLDEN's 5.0× via `if (goldenZigActive) 5.0 else 1.0`. R3-02: 3 entries asserting THORN_DAMAGE reflects damage on melee via plumbed attacker reference (50×·20 raw → 10 HP enemy delta), THORN scales linearly with thornPercent (direct value assertions — 0×5==0 ratio check would pass trivially pre-fix), and LIFESTEAL emits visible `+X HP` FloatingText when accumulated heal crosses 1 HP threshold. Helpers: `setChronoActive` / `readFortuneMultiplier` reflection on private fields; `activateGoldenZigForTest` uses public `initUWs`+`activateUW` path; `invokeUpdateUWs` reflectively expires UWs without ticking the wave spawner; `simulateEnemyMovement` fresh-engine baseline-vs-chrono comparator. R3-02 helpers: `freshEngineWithStats(ResolvedStats)` variant of `freshEngine`; `invokeOnMeleeHit` reads `WaveSpawner.onMeleeHit` field reflectively and calls it directly with a known attacker; `createDummyAttacker` constructs a stationary BASIC enemy at the ziggurat origin; `invokeOnProjectileHitEnemy` reflectively invokes the private `onProjectileHitEnemy(ProjectileEntity, EnemyEntity)`; `readPendingFloatingTextSnippets` pulls `EffectEngine.pendingEffects` reflectively and returns the text content of any `FloatingText` entries. Reflection used to invoke the private `tickRecoveryPackages(Float)` and `updateUWs(Float)` helpers — bypasses full game-loop side effects (enemy spawn, melee hits) for deterministic assertions. #125: 2 `R125` guards for the getAliveEnemies allocation refactor — (1) returns only alive EnemyEntity instances (excludes dead + non-enemy entities); (2) overlapping BLACK_HOLE+POISON_SWAMP fire each enemy's `onDeath` exactly once (the no-double-credit invariant that forbids caching the alive list across the frame, since `takeDamage` re-fires `onDeath` on a dead enemy). Helpers: `makeStationaryEnemy(zig, hp, onDeath)`, `flushPendingAdd` (reflectively drains pendingAdd into entities), `invokeGetAliveEnemies` (reflective private-method call). #146: 3 `R146` guards for the enemy-counter-drifts-negative fix — (1) a SCATTER death's children are counted by `aliveEnemyCount()` (not bypassed) and the count settles to 0 once all are killed; (2) two projectiles hitting one enemy in a single sweep fire its `onDeath` exactly once (the `!isAlive` guard); (3) a second projectile on a corpse does not re-credit the kill cash. +`engineDeathHandler(eng)` helper returning an `onDeath` that routes through the engine's real private `handleEnemyDeath`.
presentation/battle/BattleAnnouncerTest.kt        # #214: 11 pure-JVM tests for battleAnnouncement — pre-round null (blank phase / maxHp<=0), wave/phase transitions, health announced only on 25%-bracket crossings (not every tick), round-over + battle-error priority, sustained-error no re-announce, unchanged→null, healthBucket guards.
presentation/battle/biome/BiomeThemeTest.kt       # All 5 biome palettes, ziggurat colors, particles
data/sensor/StepRateLimiterTest.kt                # Normal/burst caps, window expiry, edge cases
data/sensor/StepVelocityAnalyzerTest.kt           # Natural/constant/jump patterns, window eviction
data/sensor/StepIngestionPreferencesTest.kt        # Heartbeat write/read, day-start counter, day rollover
data/sensor/StepIngestionTest.kt                   # Worker/service coordination: no double-credit, gap recovery, heartbeat gating
service/SupplyDropNotificationManagerTest.kt       # #216: 5 Robolectric tests for the supply-drop quiet-hours + daily-cap gate (drives the injected FakeTimeProvider + a UTC default zone): posts outside quiet hours under cap; suppressed inside quiet hours; enforces the 3/day cap (counter not advanced past it); resets the count on a new local day; respects the existing isSupplyDropsEnabled() gate. Asserts via shadowOf(NotificationManager).size().
data/sensor/DailyStepManagerTest.kt                 # Widget balance, walking mission progress, activity-minute idempotency; STEP_MULTIPLIER curve; #251 R251 cases for recordTrustedSteps (large gap not clamped > 250, 50k ceiling, multiplier applies, no rate-rejection via verify(antiCheatPrefs, never()), non-positive no-op); #211 R211 integration case proving a rollback baseline suppresses the daily-login gem award at the manager level (single-owner evaluate→persist→isRollback wiring)
data/sensor/DailyStepManagerConcurrencyTest.kt      # #120: credit read-check-write under a non-reentrant Mutex — concurrent credits don't lose updates
data/sensor/DailyStepManagerErrorReportingTest.kt   # #232: a hand-written ThrowingWalkingEncounterRepository makes the supply-drop pipeline stage throw; asserts the step credit still commits AND the failure surfaces via the onPipelineError seam (not swallowed). Pure JVM (no Robolectric).
data/anticheat/AntiCheatPreferencesTimeBaselineTest.kt # #211 (ADR-0036): Robolectric — TimeBaselineSource round-trip (readTimeBaseline null before any write; write-then-read round-trips all four TimeBaseline slots; currentTimeReading returns non-negative monotonic + wall values). 3 tests.
data/healthconnect/StepCrossValidatorTest.kt      # Graduated response levels, offense tracking, escrow
data/healthconnect/ActivityMinuteValidatorTest.kt # Duration/type/micro-session filtering
presentation/battle/effects/ParticlePoolTest.kt   # Acquire/release/recycle/expire/clear/reset
presentation/battle/effects/ProjectileTrailThrottleTest.kt # #243: 4 pure-JVM tests for advanceTrail — emission count tracks elapsed sim-time (not call count), 4× doesn't multiply per-sim-second emissions, single-tick never double-emits, subtract-interval carries the remainder (no drift).
presentation/battle/entities/OrbEntityTest.kt     # 6 #54-regression tests covering radial-oscillation behaviour: inner-sweep hits melee enemy, outer-sweep does not hit, HIT_COOLDOWN gates double-hits within one cooldown window, dead enemies skipped, damage forwarded verbatim from constructor to onHitEnemy callback, full MIN/MAX cycle traversed over ORBIT_PERIOD_SEC. Robolectric + JUnit 4 because OrbEntity initialises an [android.graphics.Paint] field at construction; pattern matches DailyStepDaoTest / GameSurfaceViewTest. Closes the gap that pre-fix had zero tests on the orb damage path despite the path being broken since Plan 10b.
presentation/battle/effects/ScreenShakeTest.kt    # Trigger/decay/override/reset/offset
presentation/battle/effects/DeathEffectTest.kt    # Particle counts per enemy type (6 types)
balance/StepEconomyTest.kt                        # Step economy vs 5 GDD player profiles (multi-week)
balance/CostCurveTest.kt                          # Workshop cost curve validation (standard/premium/ROI)
balance/EnemyScalingTest.kt                       # Enemy HP/damage scaling difficulty ramp
balance/TierProgressionTest.kt                    # Tier unlock timeline vs GDD §14
balance/CashEconomyTest.kt                        # In-round cash flow and interest balance
balance/CardBalanceTest.kt                        # Card tradeoffs and power levels
balance/UWBalanceTest.kt                          # UW cooldowns + damage proportionality (R4-01: renamed from UWOverdriveBalanceTest.kt; 2 overdrive-specific tests dropped, 3 surviving tests cover UW cooldown spacing / Death Wave Lv5 vs wave-50 boss / GOLDEN 5× cash bounding. Future R4-06 will add per-path balance coverage.)
balance/SupplyDropEconomyTest.kt                  # Drop rates and premium currency income
domain/usecase/TrackDailyLoginTest.kt              # Daily login PS/Gem streak awards. #211: + R211 cases proving an `isRollback = true` verdict writes nothing for the tampered date (no streak/gem/season-bonus, no row).
domain/usecase/TrackWeeklyChallengeTest.kt         # Weekly step challenge PS awards
domain/usecase/AwardBattleStepsTest.kt             # 9 tests for AwardBattleSteps: per-wave battle-step crediting, escrow/cap interaction
domain/usecase/AwardBossPowerStonesTest.kt         # 9 tests for AwardBossPowerStones: boss-kill PS award, once-per-boss guard
domain/usecase/ClaimMissionTest.kt                 # 3 tests for ClaimMission: guarded mark-first claim, idempotent repeat-claim guard (#122)
presentation/stats/StatsViewModelTest.kt           # Stats VM: profile mapping, bars, period switching
presentation/weapons/UltimateWeaponViewModelTest.kt # UW VM: display, owned/locked, balance
presentation/supplies/UnclaimedSuppliesViewModelTest.kt # Supplies VM: drops, claim, claimAll
presentation/workshop/WorkshopViewModelTest.kt     # Workshop VM: categories, purchase, quick invest
presentation/workshop/UpgradeValueLabelTest.kt     # (#29) formatPowerPerKStepsLabel: "+X.X% power / 1,000 steps" shape, +0.1% floor for sub-0.05 positives, Locale.ROOT decimal separator; pure-JVM.
presentation/cards/CardsViewModelTest.kt           # Cards VM: display, equip, upgrade
presentation/cards/CardsScreenTest.kt              # (#253) first Compose UI tests — renders real CardsScreen via createComposeRule() under Robolectric/@GraphicsMode(NATIVE) on the JVM lane, fake-backed CardsViewModel (no Hilt). 4 tests: gem balance + owned count, the 3/3 equipped cap message, pack-button enabled/disabled by affordability, opening an affordable pack grants a card. main=UnconfinedTestDispatcher so WhileSubscribed(5000) collection runs eagerly + waitForIdle(). #234: its `vm()` helper now passes a `SavedStateHandle()` (CardsViewModel gained the ctor param).
presentation/CrashReportIntentTest.kt              # (#374) Robolectric JVM test of the extracted top-level buildCrashReportIntent(): asserts ACTION_SENDTO + mailto scheme + support recipient + subject/body carry exceptionClass/message/stackPreview. @Config(sdk=[34], application=Application::class) per repo convention (avoids the targetSdk-36 vs robolectric-4.16.1 mismatch + Hilt app boot). 1 test.
presentation/onboarding/OnboardingScreenTest.kt    # (#253) Compose UI tests for the first-launch carousel — mock-backed OnboardingViewModel (sensor/battery wrappers have no fakes). 4 tests: first slide + page-position contentDescription (a11y), Skip jumps to the final slide WITHOUT calling onFinished, final-slide CTA branches "Enable step counting" (ungranted+sensor) vs "Start playing" (granted+sensor+battery-exempt). A Button merges its child Text → use onAllNodesWithText(...).onFirst() for CTA assertions.
presentation/home/HomeScreenTest.kt                # (#253 PR #316) Compose UI tests for the Home screen — fake-backed HomeViewModel. 4 tests: today-steps + best-wave render, BATTLE button, first-walk prompt shown/hidden.
presentation/workshop/WorkshopScreenTest.kt        # (#253 PR #318) Compose UI tests for WorkshopScreen — fake-backed WorkshopViewModel. 3 tests: balance + upgrade list render, upgrade card disabled when broke, purchase increments level.
presentation/store/StoreScreenTest.kt              # (#253 PR #318) Compose UI tests for StoreScreen — fake-backed StoreViewModel. 3 tests: gem balance + section headers, Buy buttons present, ad-removed shows "Purchased".
presentation/labs/LabsScreenTest.kt                # (#253 PR #318) Compose UI tests for LabsScreen — fake-backed LabsViewModel. 3 tests: slot count render, Start disabled when broke, active research shows Rush button.
presentation/missions/MissionsScreenTest.kt        # (#253 PR #318) Compose UI tests for MissionsScreen — fake-backed MissionsViewModel. 3 tests: Daily Missions header + progress, Claim button on completed mission, milestones section.
presentation/weapons/UltimateWeaponScreenTest.kt   # (#253 PR #318) Compose UI tests for UltimateWeaponScreen — fake-backed UltimateWeaponViewModel. 3 tests: power stones + weapon list, unlock disabled when broke, 3/3 equip cap message.
presentation/supplies/UnclaimedSuppliesScreenTest.kt # (#253 PR #318) Compose UI tests for UnclaimedSuppliesScreen — fake-backed UnclaimedSuppliesViewModel. 3 tests: drop list + Claim buttons, empty state, Claim All present.
presentation/battle/ui/BattleControlRailTest.kt    # (#253 PR #318) Compose UI tests for the extracted BattleControlRail composable (no VM). 2 tests: speed/pause/upgrade buttons render with content descriptions, pause callback fires.
presentation/labs/LabsViewModelTest.kt             # Labs: start research, unlock slot (use-case level)
presentation/home/HomeViewModelTest.kt             # Home VM: profile mapping, tier, drops, missions. +2 #55 regression tests: `R55 background research completion credits the COMPLETE_RESEARCH daily mission` (positive — expired research + fresh mission row → progress=1, completed=true, level incremented) and `R55 no in-flight research means COMPLETE_RESEARCH mission stays at progress 0 (R3-03 regression guard)` (defensive — confirms #55 fix doesn't re-introduce the R3-03 false trigger).
presentation/battle/BattleViewModelTest.kt         # Battle VM: init stats, biome, overdrive, ads, toggles. RO-12 adds 3 regression tests + `installEngineForPurchase` reflective helper (engine + cash field): `RO12 in-round HEALTH purchase preserves HEALTH_RESEARCH lab bonus` (Bug 1 direct guard, post-purchase maxHealth = BASE × lab-multiplier × in-round-multiplier instead of pre-fix BASE × in-round-only), `RO12 in-round HEALTH purchase preserves WALKING_FORTRESS card bonus` (Bug 2 direct guard, same shape with card multiplier instead of lab), `RO12 in-round HEALTH purchase preserves both lab AND card bonuses stacked` (combined regression matching the screenshot scenario where pre-fix dropped a 1.20 × 1.50 = 1.80× multiplier on every in-round purchase).
presentation/battle/GameSurfaceViewTest.kt         # Robolectric regression suite for [GameSurfaceView] surface lifecycle (R3-01 / GitHub issue #2; #245). 8 tests: surface recreation preserves engine progress mid-round; setSpeedMultiplier persists pendingSpeed; setPaused persists pendingPaused; initEngineIfNeeded runs engine init when no progress (inverse no-regression guard); #245 — a recreated surface re-points the engine at a FRESH SoundManager; ensureSoundManager without a prior release is a no-op; engine.soundManager is dropped (null) in the destroyed window; the recreated SoundManager re-reads the persisted mute pref. JUnit 4 + Robolectric matching DailyStepDaoTest pattern — [GameSurfaceView] extends [android.view.SurfaceView] which can't be exercised by pure-JVM tests.
presentation/missions/MissionsViewModelTest.kt     # Missions: generation, claim, milestones (use-case level)
presentation/economy/CurrencyDashboardViewModelTest.kt # Economy VM: weekly, streak, balances
presentation/store/StoreViewModelTest.kt           # Store VM: gems, cosmetics, purchase; #249 purchase-error/pending surfaces via userMessage, success is quiet
presentation/ui/EnumDisplayNameTest.kt             # 3 tests for the shared UPPER_SNAKE_CASE → "Title Case" formatter extracted from CardsScreen/LabsScreen/UnclaimedSuppliesScreen (reuse finding): single-token, multi-token, verbatim legacy-behaviour match
presentation/ui/NoRawEnumNameInUiTest.kt           # #225: pure-JVM source-scan guard (mirrors DomainPurityTest). Walks presentation/ and FAILS on any `.name.replace(` site — raw SCREAMING_CASE enum names must render via toDisplayName(). 1 test.
presentation/ui/PrivacyPolicyUrlTest.kt            # #240: drift guard — pins PRIVACY_POLICY_URL to the Data-Safety-form-declared URL so the in-app link, the form, and the hosted page can't diverge. 1 test.
presentation/ui/theme/ContrastTest.kt              # #213: 2 pure-JVM WCAG-ratio guards (relative-luminance math over plain ARGB Ints) — OnGoldArgb on GoldArgb ≥ 4.5:1 (AA-normal), and the pre-#213 DeepBronze-on-Gold documented < 4.5:1. Reads the real Color.kt consts so a token regression fails the build.
presentation/FlowCollectionLifecycleTest.kt        # #235: pure-JVM source-scan guard. Walks presentation/ and FAILS on any plain `.collectAsState()` outside the documented allowlist (BattleScreen — full-screen, owns its game loop). Enforces collectAsStateWithLifecycle() on data-backed screens so backgrounded collectors stop recomputing. 1 test.
presentation/home/HomeFirstWalkPromptTest.kt       # #224: 4 tests for HomeUiState.showFirstWalkPrompt — shows for a new player (0 steps today, <100 balance); hidden once walked, for a stockpiled returning player, and while loading/error.
presentation/supplies/SupplyRewardFormatTest.kt    # 6 tests (#20): CARD_COPY drop renders resolved card name + "x1" (not the raw type-index as a quantity); Steps/Gems/Power Stones keep the "+N <label>" shape; resolves the same CardType ClaimSupplyDrop awards
presentation/ux/CurrencyGuardTest.kt               # Currency spend clamps to 0 (gems, PS, steps)
presentation/ux/UserFeedbackTest.kt                # Workshop purchase failure sets userMessage
presentation/DeepLinkRoutingTest.kt                # Deep-link intent extra extraction
data/local/RoomSchemaTest.kt                       # Room v9 schema round-trip (profile, steps, workshop, billing_receipt) — billing_receipt round-trip added in C.5 PR 1 exercises every column
data/local/BillingReceiptDaoTest.kt                # 7 tests for BillingReceiptDao: upsert/get round-trip, grantOnceAtomic flip + runs walletCredit, idempotency (second call returns false + walletCredit skipped), markConsumed/markAcknowledged target-only, getGrantedButUnresolved filter, getAll DESC order (C.5 PR 1)
data/local/DatabaseKeyManagerTest.kt               # SQLCipher passphrase wipe path (Robolectric; AndroidKeyStore unshadowable): wipeDatabaseFile deletes db/-shm/-wal + no-op on fresh install. #238: + 2 pure decideOnDecryptFailure tests (alias absent → Wipe; alias present → Rethrow). @After resets the JVM-global keystoreAliasExists seam.
data/local/GuardedClaimDaoTest.kt                  # #122: guarded-claim SQL against real in-memory SQLite — walking-encounter + daily-mission `markClaimed` return 1-then-0 (idempotent), guarded currency deducts return rows-affected reflecting sufficiency
data/local/AtomicDaoConcurrencyTest.kt             # #252: the atomic DAOs under REAL concurrent contention (the serial GuardedClaimDaoTest can't open the race window). 12 threads + CountDownLatch start-gate on a FILE-backed Room DB (real WAL pool — :memory: is a single connection that serializes, proving nothing). 7 tests, invariant-based (exactly-one-winner, never over-spent/double-credited): adjustStepBalanceIfSufficient / spendGemsAtomic / spendPowerStonesAtomic (single-statement guards), WalkingEncounter+DailyMission markClaimed (one-shot), MilestoneDao.claimMilestoneAtomic + WorkshopDao.purchaseUpgradeAtomic (@Transaction composites). Mutation-verified: removing @Transaction from claimMilestoneAtomic → 11/12 double-credit. DB is plain Room (SQLCipher is DI-only), so no passphrase needed.
data/local/PremiumSpendDaoTest.kt                  # #236/ADR-0027: real-Room (Robolectric, in-memory) tests for the premium spend+grant @Transaction methods — openCardPackAtomic (deduct+3 cards atomic; free pack; rollback on insufficient) + unlockWeaponAtomic (deduct+unlock; fresh/existing row; rollback on insufficient; no double-deduct on re-unlock)
service/ReconcileBillingSafelyTest.kt              # #250/ADR-0028: pure-JVM tests for the reconcileBillingSafely helper — calls reconcile once; swallows a thrown reconcile (best-effort); time-bounds a hanging reconcile (withTimeoutOrNull, virtual clock)
data/local/DailyMissionDaoTest.kt                  # #127: 4 real-Room (Robolectric, in-memory) tests for the daily-mission uniqueness guard — duplicate (date,missionType) insert collapses to one row, distinct missionTypes for a date all persist, two raced full batches yield 3 not 6 (the bug repro), generateForDate idempotent. Mirrors GuardedClaimDaoTest pattern; the fakes can't model the unique index / IGNORE so this validates real SQL.
data/local/Migration11To12Test.kt                 # #127: first migration test in the project (also closes audit Low #23). 3 tests driving AppMigrations.MIGRATION_11_12.migrate() directly against a v11-shaped daily_mission table via FrameworkSQLiteOpenHelper (no MigrationTestHelper/asset wiring): 6→3 dedup + unique-index creation + post-migration duplicate-insert rejection; furthest-progress carried forward; claim-resurrection guard (MAX(claimed) keeps a claimed higher-id duplicate's state). Both dedup-correctness tests mutation-verified.
data/local/DataTransformMigrationsTest.kt          # #222 (DATA-1/TEST-4): characterization tests for the two data-transform migrations that had no upgrade-path coverage. 2 tests (same direct-migrate() pattern as Migration11To12Test): 9→10 UW single-level → 3-path integer-division split (5→2/2/1, 10→4/3/3, 1→1/0/0) + isUnlocked derive + daily_step_record bossPsEarnedToday add; 10→11 card dust→copy dedup (MAX(level)/MAX(isEquipped)/COUNT(*) copyCount) + cardType unique index + duplicate-insert rejection + cardDust zero-out.
data/local/MigrationChainTest.kt                  # #237: guards AppMigrations.validateChain + pins the live chain. 1 Robolectric test (real ALL + built-DB version == 12, no problems) + 6 pure-logic cases (valid chain; missing top migration = forgotten-registration; mid-chain gap; multi-step jump; not-starting-at-floor; empty set). Reads version via db.openHelper.readableDatabase.version (the @Database annotation is @Retention(CLASS), so reflection returns null).
data/local/DailyStepDaoTest.kt                     # 13 tests for DailyStepDao: regression guard for `incrementBattleSteps succeeds on empty table` (NOT NULL-before-ON-CONFLICT semantic), creditBattleStepsAtomic on empty table credits wallet + populates all Kotlin-default columns, creditBattleStepsAtomic preserves existing sensor data (ON CONFLICT UPDATE branch), partial credit near cap, zero credit when cap exhausted. #121: +9 column-targeted-upsert guards — per-method create-on-empty-table (set{SensorAndCreditedSteps,HealthConnectSteps,ActivityMinutes,Escrow}), HC write preserves sensor/credited columns, the core lost-update repro (4 disjoint-column writers from a stale snapshot all survive), battle/boss-counter preservation, SET-not-increment semantics. Robolectric + real in-memory Room DB, mirrors BillingReceiptDaoTest pattern (2026-05-12 hotfix)
data/billing/BillingManagerImplTest.kt             # 20 tests for BillingManagerImpl (C.5 PR 1): 3 happy paths (GEM_PACK_SMALL consume + AD_REMOVAL ack + SEASON_PASS sub with 30-day expiry), 5 failure paths (user-cancel, product-unavailable, no-activity, connect-fails, pending purchase), idempotency (same purchaseToken does not double-credit), 2 reconciliation cases (PENDING→PURCHASED transition + retryUnresolvedConsumeOrAck without re-credit), isAdRemoved / isSeasonPassActive delegation. Robolectric + real in-memory Room DB for @Transaction semantics + mockito-kotlin for adapter.
data/billing/internal/RealPurchaseVerifierTest.kt  # 9 tests for RealPurchaseVerifier (#124, ADR-0005 amendment): RSA signature verification of Play purchase JSON, product+token binding, tampered-signature rejection, blank-key fail-open (debug/CI only)
data/ads/AdRequestConfigurationTest.kt             # #241/ADR-0032: 3 tests pinning buildAdRequestConfiguration() — maxAdContentRating=PG + child-directed/under-age tags UNSPECIFIED (the 13+/no-flag decision). Plain JVM (relies on unitTests.isReturnDefaultValues=true to absorb the internal Log.w in setMaxAdContentRating).
data/ads/RewardAdManagerImplTest.kt                # 8 tests for RewardAdManagerImpl (C.6 PR 1 / ADR-0006): happy Rewarded; Cancelled on user-dismiss; 4 Error paths (no activity, consent unavailable, load failed with AdMob NO_FILL code 3 → 'No ad available' user msg, show failed with code 1 → 'already shown' user msg); consent-denied-still-grants per Q1; placement → ad-unit routing for all 3 AdPlacements. No Robolectric — plain-Kotlin sealed adapter types + mockito-kotlin.
data/integration/EscrowLifecycleTest.kt            # End-to-end escrow lifecycle (release + discard)
data/repository/CosmeticRepositoryImplTest.kt      # Seed → ZIGGURAT_COLOR_LOOKUP → overrideColors mapping for all 5 palette-shipping cosmetics (zig_jade, lapis_lazuli_skin, garden_ziggurat_skin, sandals_of_gilgamesh, zig_obsidian) with exact-value assertions as content-as-code contracts; non-palette seeds have null overrideColors; equipped zig_jade surfaces via observeEquipped with palette intact; ensureSeedData idempotent; partial-catalogue upgrade inserts 4 missing milestone cosmetics without touching legacy 7; existing-row isOwned/isEquipped preserved across ensureSeedData (C.2 PR 2 + PR 3 + PR 3b + PR 3c + ensureSeedData fix)
data/repository/WorkshopRepositoryImplTest.kt      # 4 tests for WorkshopRepositoryImpl.ensureUpgradesExist (issue #20 fix): empty-table seeds every UpgradeType; upgrade-from-v8 install adds RAPID_FIRE while preserving historical levels; idempotent on second call; orphan/unknown rows tolerated and not deleted. Mockito-kotlin `mock<PlayerProfileDao>()` to satisfy constructor signature; ensureUpgradesExist never invokes player DAO methods.
data/repository/LabRepositoryImplTest.kt           # 3 tests for LabRepositoryImpl.ensureResearchExists (issue #20 sibling fix): empty-table seeds every ResearchType; upgrade-from-v8 install adds MULTISHOT_RESEARCH + BOUNCE_RESEARCH while preserving historical levels AND active-research timestamps (startedAt / completesAt); idempotent on second call.
data/repository/PlayerRepositoryImplTest.kt        # 18 tests for PlayerRepositoryImpl (V1X-07): wallet/profile/tier observe mappings, addGems/addPowerStones multi-call delegation, V1X-10 atomic spend regression guards (spendGems → spendGemsAtomic only, never adjustGems + incrementGemsSpent fallback), updateBestWave read-modify-write merging, ensureProfileExists conditional upsert, getStepBalance null-safe.
data/repository/UltimateWeaponRepositoryImplTest.kt # 12 tests for UltimateWeaponRepositoryImpl (V1X-07): observeUnlockedWeapons isUnlocked filter (R4-06 contract), observeEquippedWeapons defensive isUnlocked + isEquipped intersection, unlockWeapon insert-vs-markUnlocked branching, R4-06 per-path upgrade dispatch (DAMAGE → updateDamageLevel; SECONDARY → updateSecondaryLevel; COOLDOWN → updateCooldownLevel), defensive insert when row missing, equip/unequip isEquipped flag flips with no-op when entity missing.
data/repository/CardRepositoryImplTest.kt          # 15 tests for CardRepositoryImpl (V1X-07): observeAllCards/observeEquippedCards mappings, hasCard true/false, addCard insert returns id, R4-08 addCardOrIncrementCopy increment-vs-insert branching, decrementCopiesAndLevelUp returns Boolean from DAO row count, upgradeCard / equipCard / unequipCard / deleteCard with no-op-when-missing guards.
data/repository/StepRepositoryImplTest.kt          # 9 tests for StepRepositoryImpl (V1X-07): observeTodayRecord mapping + null emission, getDailyRecord round-trip, releaseEscrow / discardEscrow delegation to DAO clearEscrow. #121: the 4 per-field updates now verify delegation to the column-targeted DAO methods (updateDailySteps→setSensorAndCreditedSteps, updateHealthConnectSteps→setHealthConnectSteps, updateActivityMinutes→setActivityMinutes, updateEscrow→setEscrow) + assert upsert() is never called (column-preservation behaviour itself is proven against real SQL in DailyStepDaoTest).
data/DataDeletionManagerTest.kt                    # 6 Robolectric tests for DataDeletionManager (V1X-01): clears all 15 SharedPreferences in PREFS_NAMES (biome / milestone-notification / notification / sound / music / anti-cheat / step-ingestion / db-key / widget-data / smart-reminders / billing-anti-fraud / crash-breadcrumb #190 / onboarding #247 / haptics #247 / supply-drop-notifications #216); closes the in-memory Room DB; calls activity.recreate() exactly once; idempotent across repeated invocations; clears the crash breadcrumb prefs. #248: + cancels in-flight (ENQUEUED, long initial delay) work before close — asserts WorkInfo.State.CANCELLED + DB closed. Uses `WorkManagerTestInitHelper.initializeTestWorkManager` with a `SynchronousExecutor` (so the awaited cancel resolves on the calling thread). Test dep: `androidx.work:work-testing`.
data/DataDeletionPrefsCoverageTest.kt              # #247: pure-JVM source-scan guard (mirrors DomainPurityTest). Walks app/src/main, discovers every getSharedPreferences(...) call site (string literal + same-file `const val`), and FAILS if a discovered prefs file is missing from DataDeletionManager.PREFS_NAMES (incomplete wipe) OR if an arg can't be resolved (unverifiable) — so the wipe list can't silently drift.
domain/battle/engine/SimulationMathTest.kt         # 42 pure-JVM tests for SimulationMath (V1X-09 Phase 1, ADR-0012; V1X-18 ADR-0015 added 8 stepMultiplierBonus tests; #126 added 7 clampAccumulator tests). Recovery pulse formula at L0/L1/L25/L60/min-1; chrono active/inactive/custom-factor; thorn reflection edge cases (zero rawDamage / zero thornPercent / condition multiplier); lifesteal heal cap (15 %) + sub-1 fraction conservation; lifesteal accumulator threshold-crossing (no-burst < 1 / 1-HP burst / multi-HP burst / 50-hit accumulation); HP clamp (over-heal / negative / passthrough / zero maxHp / negative maxHp). V1X-18 stepMultiplierBonus L0/L1/L10/L20/L50/L100/L200 + L99-vs-L100 dead-content regression guard. #126 clampAccumulator: sub-tick passthrough / healthy multi-tick burst / runaway cap / exact-ceiling boundary / remainder preservation / zero / MAX_CATCHUP_TICKS bound contract.
domain/battle/engine/SimulationTest.kt             # 35 pure-JVM tests for Simulation (V1X-09 Phase 3, ADR-0012, slices 1–6). Slice 1 cash economy (9): creditCash accumulates both balances + sums + ignores non-positive; applyInterest adds to cash only (not lifetime) + caps at 10 % + level-0 no-op; spend deducts when affordable + rejects/leaves balance untouched when insufficient; reset zeroes both. Slice 2 round-progress (7): tickElapsed accumulates the clock; recordEnemyKilled increments; creditSteps accumulates + ignores non-positive; hasWaveProgress false-on-fresh / true-after-tick / true-after-kill; reset zeroes the counters. Slice 3 tickEntities (4): scales dt for a chrono-slowable fake, full dt for a non-slowable fake, factor-1 no-op, mixed-list all-ticked-once (private FakeEntity impl of EntityProtocol). Slice 4 collision sweep (6): detectProjectileEnemyHits fires the pair on overlap / stops at first overlapping enemy (break) / no-fire when nothing overlaps / interleaved-firing guard (onHit moves the enemy mid-sweep so a later projectile misses); detectZigguratHits fires on overlap / no-fire outside the radius. Slice 5 UW lifecycle (7): advanceUWTimers cooldown decrement+clamp-at-0 / zero-cooldown+inactive-effect untouched / active-effect countdown without expiry / justExpired+clamp on the crossing frame with effectWasActive still true; isUWReadyToFire true off-cooldown-and-idle / false on cooldown / false mid-effect. Slice 6 events stream (2): emit delivers both event types (StepReward + BossKilled) to a collector in order; replay=0 no-replay contract (a late subscriber sees nothing — guards the playAgain collector against double-crediting the previous round). No Robolectric.
domain/battle/entity/ProjectileStateTest.kt        # 4 pure-JVM tests for ProjectileState (V1X-09 Phase 2, ADR-0012): single-axis proportional move, two-axis proportional split, arrival-within-one-step death, multi-step convergence. No Robolectric — the motion math is now pure-domain.
domain/battle/entity/OrbStateTest.kt               # 4 pure-JVM tests for OrbState (V1X-09 Phase 2, ADR-0012): mid-radius + on-orbit position at phase 0, MAX after quarter-cycle, MIN after three-quarter-cycle, angularSpeed advances the orbit angle. No Robolectric (the OrbEntity Robolectric suite still covers the enemy-proximity/hit-cooldown path).
domain/battle/entity/EnemyStateTest.kt             # 5 pure-JVM tests for EnemyState (V1X-09 Phase 2, ADR-0012): melee proportional move (no attack), fire-on-arrival when in range, attack-cooldown gating across the interval (true/false/true), ranged stops + attacks farther out than melee range (initialDist*0.4 = 80 > 40), knockback shift. No Robolectric.
domain/battle/entity/ZigguratStateTest.kt          # 6 pure-JVM tests for ZigguratState (V1X-09 Phase 2, ADR-0012, final entity): regen below cap, regen clamp at maxHp, attack-cooldown gating across the interval (true/onFired/false/true), holdReady keeps ready next tick, attackInterval reflects attackSpeed × rapidFireMultiplier, updateStats redirects attackRange + attackInterval. No Robolectric.
presentation/audio/MusicManagerNullPlayerTest.kt    # #246 + #242/ADR-0033: 8 Robolectric tests (inject a synchronous decode executor + idle the main looper). #246: playerFactory→null degrades to silent (no crash) + happy-path isLooping/setVolume/start. #242: each track built at most once across repeated navigations (counting factory); switch pauses outgoing + seekTo(0)/start incoming; build-once-or-in-flight (deferred executor: B→W→B mid-flight decodes B once); muted manager doesn't start a deferred-decoded player; release frees both players and a late (post-release) decode releases-not-starts.
presentation/audio/SoundManagerThrottleTest.kt     # 5 pure-logic tests for the V1X-05 frequency-aware SHOOT throttle formula `(expectedIntervalMs / 3).coerceIn(30, 100)`. Default 100ms input → 33ms throttle; baseline 1000ms (1 atk/sec) caps at 100ms; fast 200ms (5 atk/sec) → 66ms; very-fast 60ms floors at 30ms; zero floors at 30ms. Pure JVM, no Robolectric needed.
presentation/audio/MusicPreferencesTest.kt         # 1 Robolectric test for MusicPreferences round-trip (V1X-06): default isMuted=false + getVolume=0.5; setMuted(true) + setVolume(0.8) round-trip persists across SharedPreferences read.
service/StepWidgetProviderTest.kt                  # Widget SharedPreferences round-trip
service/StepBalanceDisplayTest.kt                  # 4 tests (#43): resolveDisplayBalance folds a freshly-read balance against last-known-good and never coerces a failed read to 0 (a genuine zero is still shown); guards the always-on notification against flashing "Balance: 0" on a transient repo failure
service/StartForegroundSafelyTest.kt               # #244: 3 pure-JVM tests for the top-level startForegroundSafely(start,onFailure) seam — a clean start returns true (no onFailure); an IllegalStateException (FGS-not-allowed supertype) and a SecurityException are caught, reported to onFailure, and return false (caller stopSelf()s instead of the process dying).
service/BootReceiverTest.kt                        # #217: 3 Robolectric tests for the BootReceiver Android glue (NOT @AndroidEntryPoint → no Hilt). Drives onReceive + asserts via ShadowApplication: a non-BOOT_COMPLETED action starts no service; BOOT_COMPLETED with ACTIVITY_RECOGNITION denied starts no service; BOOT_COMPLETED + granted starts a service Intent targeting StepCounterService (this package). Covers the action gate + permission gate + service-dispatch the pure seams don't.
service/StepCounterServiceTest.kt                  # #217: 2 Robolectric tests for the injection-independent StepCounterService lifecycle on a directly-constructed instance — onStartCommand returns START_STICKY (restart-after-kill contract); onBind returns null (started, not bound). The Hilt-injected onCreate path stays on its pure seams (StartForegroundSafelyTest/StepBalanceDisplayTest) + the instrumented suite.
```

### Instrumented (androidTest) source set

All paths relative to `app/src/androidTest/java/com/whitefang/stepsofbabylon/`. Stood up by V1X-08 Phase 1A on 2026-05-28.

```
HiltTestRunner.kt                  # AndroidJUnitRunner subclass; overrides newApplication() to install HiltTestApplication. Wired via testInstrumentationRunner in app/build.gradle.kts. The class FQN com.whitefang.stepsofbabylon.HiltTestRunner is the contract between Gradle config and runtime instantiation — don't rename without updating both call sites.
InfrastructureSmokeTest.kt         # Single @HiltAndroidTest harnessBoots smoke test asserting the instrumented pipeline (Gradle config → KSP → Hilt → AndroidJUnit4 → emulator) runs end-to-end. Deliberately does not touch DB / services / sensors / permissions — those belong to the three follow-up suites (BattleSurfaceLifecycleTest, StoreIapFlowTest, DeepLinkIntentTest).
BattleSurfaceLifecycleTest.kt      # First real instrumented suite (V1X-08, layered 2026-05-29). 4 @RunWith(AndroidJUnit4) tests mirroring the R3-01 backgrounding-state-loss invariants from the Robolectric GameSurfaceViewTest, but against the REAL Android framework: surface-recreation preserves engine progress (initEngineIfNeeded gated on hasWaveProgress), setSpeedMultiplier persists pendingSpeed, setPaused persists pendingPaused, fresh-engine init still runs (no-regression). Constructs GameSurfaceView on the main thread via InstrumentationRegistry + runOnMainSync. No HiltAndroidRule (GameSurfaceView takes only Context). No new gradle deps — androidx.test.runner + androidx.test.ext.junit already present from Phase 1A. Hardens the Robolectric guard, which can't exercise true SurfaceHolder.Callback lifecycle timing.
DeepLinkIntentTest.kt              # Second real instrumented suite (V1X-08, layered 2026-05-29). 4 @RunWith(AndroidJUnit4) tests verifying the `navigate_to` deep-link contract MainActivity reads (intent.getStringExtra("navigate_to") → Screen.fromRoute → argumentFreeRoutes gate) against the REAL framework: (1) the extra survives a real android.os.Parcel round-trip — the instrumented-only value over the Robolectric DeepLinkRoutingTest, since notification deep-links travel inside a PendingIntent that marshals through Binder/Parcel; (2) missing extra is null after round-trip; (3) every argumentFreeRoute resolves + passes the routing gate; (4) unknown route fails closed. No Hilt rule (touches only Intent / Parcel / pure-Kotlin Screen). No new gradle deps.
```
